local a = {
	Smoothing = 0.4,
	Prediction = getgenv().Aimlock_Prediction or 0.17,
	ShowDot = getgenv().Aimlock_ShowDot or true
}
local b = {
	Enabled = false,
	Mode = "CAM",
	Multi = false,
	CTt = nil,
	Adeptive = 0.03,
	VeC = {},
	SedT = {},
	VTs = {}
}
local c = game:GetService("Players")
local d = game:GetService("Workspace")
local e = game:GetService("RunService")
local f = game:GetService("Stats")
local g = c.LocalPlayer;
local h = d.CurrentCamera;
local i = nil;
local j = "AimLockUpdate";
local k = Enum.RenderPriority.Camera.Value + 1;
task.spawn(function()
	local a = {}
	while task.wait(5) do
		local c = nil;
		local d = nil;
		c, d = pcall(function()
			return f.Network.ServerStatsItem["Data Ping"]:GetValue()
		end)
		if c and type(d) == "number" and d >= 0 then
			table.insert(a, d / 1000)
			if # a > 20 then
				table.remove(a, 1)
			end;
			local c = 0;
			for a, a in ipairs(a) do
				c += a
			end;
			b.Adeptive = math.clamp(c / # a, 0.03, 0.23)
		end
	end
end)
local function f()
	local a = g.Character;
	local c = d:FindFirstChild("GameAssets")
	local c = c and c:FindFirstChild("Teams")
	if not c or not a then
		b.VTs = {}
		return
	end;
	local d = c:FindFirstChild("Killer")
	local c = c:FindFirstChild("Survivor")
	local e = d and a:IsDescendantOf(d)
	local a = c and a:IsDescendantOf(c)
	if e then
		b.VTs = {
			c
		}
	elseif a then
		b.VTs = {
			d
		}
	else
		b.VTs = {
			d,
			c
		}
	end
end;
local function l(a)
	local c = g.Character;
	if not c then
		return
	end;
	for c, c in ipairs(c:GetDescendants()) do
		if c:IsA("BasePart") and c.Name ~= "Replicator_Good" then
			if a then
				b.SedT[c] = b.SedT[c] or c.Transparency;
				c.Transparency = math.max(b.SedT[c], a)
			elseif b.SedT[c] then
				c.Transparency = b.SedT[c]
			end
		end
	end;
	if not a then
		table.clear(b.SedT)
	end
end;
task.spawn(function()
	local a = d:FindFirstChild("GameAssets")
	local a = a and a:FindFirstChild("Teams")
	if not a then
		return
	end;
	local function b(a)
		a.ChildAdded:Connect(function()
			task.defer(f)
		end)
		a.ChildRemoved:Connect(function()
			task.defer(f)
		end)
	end;
	for a, a in ipairs(a:GetChildren()) do
		b(a)
	end;
	a.ChildAdded:Connect(b)
end)
local function m(e)
	local f = g.Character;
	local g = f and f:FindFirstChild("Replicator_Good")
	local j = f and f:FindFirstChild("HumanoidRootPart")
	if not f or not g or not j then
		l(nil)
		if i then
			i.Transparency = 1
		end;
		return
	end;
	if b.Enabled and b.Mode == "CAM" then
		l(0.8)
	else
		l(nil)
	end;
	local k = b.CTt;
	local l = function(a)
		return a and a.Parent and a:FindFirstChild("Replicator_Good") and a:FindFirstChildOfClass("Humanoid") and a:FindFirstChildOfClass("Humanoid").Health > 0
	end;
	if b.Multi or not l(k) then
		local a = math.huge;
		k = nil;
		for b, b in ipairs(b.VTs) do
			for b, b in ipairs(b:GetChildren()) do
				if b ~= f and c:GetPlayerFromCharacter(b) and l(b) then
					local c = (b.Replicator_Good.Position - g.Position).Magnitude;
					if c < a then
						a = c;
						k = b
					end
				end
			end
		end
	end;
	if k ~= b.CTt then
		b.CTt = k;
		table.clear(b.VeC)
	end;
	local c = b.CTt and b.CTt:FindFirstChild("Replicator_Good")
	if c and b.Enabled then
		local f = j.Position;
		local g = c.Position;
		local k = os.clock()
		local l = b.VeC[c];
		if not l then
			l = {
				P = g,
				T = k,
				V = Vector3.zero
			}
			b.VeC[c] = l
		end;
		if (g - l.P).Magnitude > 0.0001 then
			local c = math.max(k - l.T, 0.001)
			local c = (g - l.P) / c;
			local a = l.P + (l.V * (a.Prediction + b.Adeptive))
			local a = (g - a).Magnitude;
			local b = 15;
			local a = math.clamp(a / b, 0, 1)
			local b = 0.15;
			local d = 0.03;
			local a = b + (d - b) * (1 - (1 - a) ^ 2)
			local a = 1 - (1 - a) ^ (e * 60)
			l.V = l.V:Lerp(c, a)
			l.P = g;
			l.T = k
		end;
		local c = (g - f).Magnitude;
		local k = math.clamp(a.Prediction + b.Adeptive, 0, 1)
		if c < 7 then
			k = math.max(0, k - ((7 - c) / 0.1) * 0.00285)
		end;
		local c = g + (l.V * k)
		if a.ShowDot then
			if not i or not i.Parent then
				i = Instance.new("Part")
				i.Name = "GayAimLockDot";
				i.Shape = Enum.PartType.Ball;
				i.Size = Vector3.new(3, 3, 3)
				i.Color = Color3.fromRGB(255, 50, 50)
				i.Material = Enum.Material.Neon;
				i.Anchored = true;
				i.CanCollide = false;
				i.CanQuery = false;
				i.CanTouch = false;
				i.CastShadow = false;
				i.Parent = d
			end;
			i.Position = c;
			i.Transparency = 0.3
		end;
		local a = math.clamp(a.Smoothing, 0.01, 0.99)
		local a = 1 - (1 - a) ^ (e * 60)
		if b.Mode == "CAM" then
			local b = h.CFrame.Position;
			if (c - b).Magnitude > 0.01 then
				h.CFrame = h.CFrame:Lerp(CFrame.lookAt(b, c), a)
			end
		else
			local b = Vector3.new(c.X, f.Y, c.Z)
			if (b - f).Magnitude > 0.01 then
				j.CFrame = j.CFrame:Lerp(CFrame.lookAt(f, b), a)
			end
		end
	else
		if i then
			i.Transparency = 1
		end
	end
end;
e:UnbindFromRenderStep(j)
e:BindToRenderStep(j, k, m)
g.CharacterAdded:Connect(function(a)
	table.clear(b.SedT)
	table.clear(b.VeC)
	b.CTt = nil;
	a:GetPropertyChangedSignal("Parent"):Connect(function()
		task.defer(f)
	end)
	task.defer(f)
end)
if g.Character then
	g.Character:GetPropertyChangedSignal("Parent"):Connect(function()
		task.defer(f)
	end)
	f()
end;
d:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
	h = d.CurrentCamera
end)
local a = {}
function a.TA()
	b.Enabled = not b.Enabled;
	if not b.Enabled then
		l(nil)
		if i then
			i.Transparency = 1
		end;
		b.CTt = nil
	end;
	return b.Enabled
end;
function a.TMode()
	b.Mode = b.Mode == "CAM" and "HRP" or "CAM";
	if b.Mode == "HRP" then
		l(nil)
	end;
	return b.Mode
end;
function a.TM()
	b.Multi = not b.Multi;
	return b.Multi
end;
return a

local a = {
	S = {
		Sm = 0.4,
		P = getgenv().Aimlock_Prediction or 0.36,
		SD = getgenv().Aimlock_ShowDot or true
	},
	St = {
		IL = false,
		LM = "CAM",
		MT = false,
		T = nil,
		IFL = false,
		VC = {},
		AP = 0
	}
}
local b = {}
local c = nil;
local c = game:GetService("Players")
local d = game:GetService("Workspace")
local e = game:GetService("RunService")
local f = game:GetService("Stats")
local c = c.LocalPlayer;
local g = d.CurrentCamera;
local h = nil;
local function i()
	local a = d:FindFirstChild("GameAssets")
	return a and a:FindFirstChild("Teams") or nil
end;
task.spawn(function()
	local b = {}
	while task.wait(5) do
		local c;
		local d;
		c, d = pcall(function()
			return f.Network.ServerStatsItem["Data Ping"]:GetValue()
		end)
		if c and type(d) == "number" and d >= 0 then
			table.insert(b, d / 1100)
			if # b > 20 then
				table.remove(b, 1)
			end;
			local c = 0;
			for a in ipairs(b) do
				local a = b[a];
				c += a
			end;
			a.St.AP = math.clamp(c / # b, 0.02, 0.12)
		end
	end
end)
local function f(a)
	if not a or not a.Parent then
		return nil
	end;
	local b = a:FindFirstChildOfClass("Humanoid")
	local a = a:FindFirstChild("HumanoidRootPart")
	if b and b.Health > 0 and a then
		return a
	end;
	return nil
end;
local function j(a)
	local c = c.Character;
	if not c then
		return
	end;
	for d in ipairs(c:GetDescendants()) do
		local c = c:GetDescendants()[d];
		if c:IsA("BasePart") and c.Name ~= "HumanoidRootPart" then
			if a then
				if b[c] == nil then
					b[c] = c.Transparency
				end;
				c.Transparency = math.max(b[c], a)
			else
				if b[c] ~= nil then
					c.Transparency = b[c]
				end
			end
		end
	end;
	if not a then
		table.clear(b)
	end
end;
local function k(b)
	if a.S.SD and b and b == b then
		if not h or not h.Parent then
			h = Instance.new("Part")
			h.Name = "GayAimLockDot";
			h.Shape = Enum.PartType.Ball;
			h.Size = Vector3.new(3, 3, 3)
			h.Color = Color3.fromRGB(255, 50, 50)
			h.Material = Enum.Material.Neon;
			h.Anchored = true;
			h.CanCollide = false;
			h.CanQuery = false;
			h.CanTouch = false;
			h.CastShadow = false;
			h.Parent = d
		end;
		h.Position = b;
		h.Transparency = 0.3
	else
		if h then
			h.Transparency = 1
		end
	end
end;
local function h(a)
	local b = i()
	if not b or not a then
		return {}
	end;
	local c = b:FindFirstChild("Killer")
	local d = b:FindFirstChild("Survivor")
	local e = b:FindFirstChild("Other")
	local b = b:FindFirstChild("Ghost")
	local b = b and a:IsDescendantOf(b)
	local a = c and a:IsDescendantOf(c)
	local f = {}
	if b then
		if c then
			table.insert(f, c)
		end
	elseif a then
		if d then
			table.insert(f, d)
		end;
		if e then
			table.insert(f, e)
		end
	else
		if c then
			table.insert(f, c)
		end
	end;
	return f
end;
local function i()
	local a = f(c.Character)
	if not a then
		return nil
	end;
	local b = h(c.Character)
	local d = nil;
	local e = math.huge;
	for g in ipairs(b) do
		local b = b[g];
		for g in ipairs(b:GetChildren()) do
			local b = b:GetChildren()[g];
			if b ~= c.Character then
				local c = f(b)
				if c then
					local a = (c.Position - a.Position).Magnitude;
					if a < e then
						e = a;
						d = b
					end
				end
			end
		end
	end;
	return d
end;
local function h(b, d)
	if not b then
		return Vector3.zero
	end;
	local e = os.clock()
	local g = b.Position;
	local h = a.St.VC[b];
	if not h then
		a.St.VC[b] = {
			LP = g,
			LT = e,
			V = Vector3.zero
		}
		return g
	end;
	local b = math.max(e - h.LT, 0.001)
	if b <= 0.25 then
		local a = (g - h.LP) / b;
		h.V = h.V:Lerp(a, math.clamp(d * 3, 0, 1))
	end;
	h.LP = g;
	h.LT = e;
	local a = math.clamp(a.S.P + a.St.AP, 0, 1)
	local b = f(c.Character)
	if b then
		local b = (g - b.Position).Magnitude;
		if b < 7 then
			a = math.max(0, a - (7 - b) / 0.1 * 0.00283)
		end
	end;
	return g + (h.V * a)
end;
local function l(b, d)
	if not b or b ~= b then
		return
	end;
	local c = c.Character;
	local e = g.CFrame.Position;
	local f = math.clamp(a.S.Sm, 0.01, 0.99)
	local d = 1 - (1 - f) ^ (d * 60)
	if a.St.LM == "CAM" then
		if (b - e).Magnitude > 0.01 then
			local b = CFrame.lookAt(e, b)
			if a.St.IFL then
				g.CFrame = b
			else
				g.CFrame = g.CFrame:Lerp(b, d)
			end
		end
	else
		local c = c and c:FindFirstChild("HumanoidRootPart")
		if c then
			local e = c.Position;
			local b = Vector3.new(b.X, e.Y, b.Z)
			if (b - e).Magnitude > 0.01 then
				local b = CFrame.lookAt(e, b)
				if a.St.IFL then
					c.CFrame = b
				else
					c.CFrame = c.CFrame:Lerp(b, d)
				end
			end
		end
	end;
	a.St.IFL = false
end;
c.CharacterAdded:Connect(function()
	table.clear(b)
	table.clear(a.St.VC)
	a.St.T = nil
end)
e.RenderStepped:Connect(function(b)
	if a.St.IL and a.St.LM == "CAM" then
		j(0.8)
	else
		j(nil)
	end;
	local c = f(c.Character)
	if not c then
		k(nil)
		return
	end;
	if a.St.T then
		local b = f(a.St.T)
		if not b then
			a.St.T = nil;
			table.clear(a.St.VC)
		end
	end;
	if a.St.MT or not a.St.T then
		local b = i()
		if b ~= a.St.T then
			a.St.T = b;
			table.clear(a.St.VC)
		end
	end;
	if a.St.T then
		local c = f(a.St.T)
		if c then
			local c = h(c, b)
			if a.St.IL then
				k(c)
				l(c, b)
				return
			end
		end
	end;
	k(nil)
end)
d:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
	g = d.CurrentCamera
end)
function a:TA()
	self.St.IL = not self.St.IL;
	self.St.IFL = self.St.IL;
	if not self.St.IL then
		j(nil)
		k(nil)
	end;
	return self.St.IL
end;
function a:TM()
	self.St.MT = not self.St.MT;
	self.St.T = nil;
	table.clear(self.St.VC)
	return self.St.MT
end;
function a:TMode()
	self.St.LM = self.St.LM == "CAM" and "HRP" or "CAM";
	self.St.IFL = self.St.IL;
	if self.St.LM == "HRP" then
		j(nil)
	end;
	return self.St.LM
end;
return a

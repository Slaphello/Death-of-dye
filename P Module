local a = game:GetService("Players")
local b = game:GetService("Workspace")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer;
local f = b.CurrentCamera;
local g = {
	C = {
		p = getgenv().Aimlock_Prediction or .36,
		s = .3,
		sd = getgenv().Aimlock_ShowDot or true,
		pc = true
	},
	S = {
		e = false,
		m = "CAM",
		ct = nil,
		tv = Vector3.zero,
		ap = 0,
		lpc = 0,
		ifl = false
	},
	V = {
		d = nil,
		ot = {}
	},
	X = {}
}
local function h(a)
	if not a then
		return nil
	end;
	local b = a:FindFirstChildOfClass("Humanoid")
	local a = a:FindFirstChild("HumanoidRootPart")
	return (b and b.Health > 0 and a) and a or nil
end;
local function i()
	local a = os.clock()
	if a - g.S.lpc > 3 then
		g.S.lpc = a;
		local a;
		local b;
		a, b = pcall(function()
			return d.Network.ServerStatsItem["Data Ping"]:GetValue()
		end)
		if a and type(b) == "number" then
			g.S.ap = math.clamp(b / 1000, .02, .15)
		end
	end
end;
function g:HV(a)
	local c = e.Character;
	if c then
		for a, a in ipairs(c:GetDescendants()) do
			if a:IsA("BasePart") and a.Name ~= "HumanoidRootPart" then
				if self.S.e and self.S.m == "CAM" then
					if self.V.ot[a] == nil then
						self.V.ot[a] = a.Transparency
					end;
					a.Transparency = math.max(self.V.ot[a], .85)
				else
					if self.V.ot[a] ~= nil then
						a.Transparency = self.V.ot[a]
					end
				end
			end
		end
	end;
	if not self.S.e then
		table.clear(self.V.ot)
	end;
	if self.C.sd and self.S.e and a then
		if not self.V.d or not self.V.d.Parent then
			local a = Instance.new("Part")
			a.Name = "V";
			a.Shape = Enum.PartType.Ball;
			a.Size = Vector3.new(3, 3, 3)
			a.Color = Color3.fromRGB(255, 50, 50)
			a.Material = Enum.Material.Neon;
			a.Anchored = true;
			a.CanCollide = false;
			a.CanQuery = false;
			a.CanTouch = false;
			a.CastShadow = false;
			a.Parent = b;
			self.V.d = a
		end;
		self.V.d.Position = a;
		self.V.d.Transparency = .3
	else
		if self.V.d then
			self.V.d.Transparency = 1
		end
	end
end;
function g:GCP()
	local b = h(e.Character)
	if not b then
		return nil
	end;
	local c = nil;
	local d = math.huge;
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= e and a.Character then
			local e = h(a.Character)
			if e then
				local b = (e.Position - b.Position).Magnitude;
				if b < d then
					d = b;
					c = a.Character
				end
			end
		end
	end;
	return c
end;
function g:CP(a, b)
	local c = a.AssemblyLinearVelocity;
	self.S.tv = self.S.tv:Lerp(c, b * 15)
	local b = self.C.p;
	if self.C.pc then
		b = b + self.S.ap
	end;
	local c = h(e.Character)
	if c then
		local a = (a.Position - c.Position).Magnitude;
		if a < 10 then
			b = math.max(0, b - ((10 - a) * .05))
		end
	end;
	return a.Position + (self.S.tv * b)
end;
function g:AA(a, b)
	local c = e.Character;
	local d = f.CFrame.Position;
	local b = 1 - (1 - math.clamp(self.C.s, .01, .99)) ^ (b * 60)
	if self.S.m == "CAM" then
		if (a - d).Magnitude > .1 then
			local a = CFrame.lookAt(d, a)
			if self.S.ifl then
				f.CFrame = a
			else
				f.CFrame = f.CFrame:Lerp(a, b)
			end
		end
	elseif self.S.m == "HRP" then
		local c = c and c:FindFirstChild("HumanoidRootPart")
		if c then
			local d = c.Position;
			local a = Vector3.new(a.X, d.Y, a.Z)
			if (a - d).Magnitude > .1 then
				local a = CFrame.lookAt(d, a)
				if self.S.ifl then
					c.CFrame = a
				else
					c.CFrame = c.CFrame:Lerp(a, b)
				end
			end
		end
	end;
	self.S.ifl = false
end;
function g:DA()
	for a, a in pairs(self.X) do
		if a then
			a:Disconnect()
		end
	end;
	table.clear(self.X)
	pcall(function()
		c:UnbindFromRenderStep("C")
	end)
end;
function g:CA()
	self:DA()
	self.X.CU = b:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
		f = b.CurrentCamera
	end)
	self.X.CA = e.CharacterAdded:Connect(function()
		table.clear(self.V.ot)
		self.S.ct = nil;
		self.S.tv = Vector3.zero
	end)
	c:BindToRenderStep("C", Enum.RenderPriority.Camera.Value + 1, function(a)
		if not self.S.e then
			self:HV(nil)
			return
		end;
		i()
		local b = h(e.Character)
		if not b then
			self:HV(nil)
			return
		end;
		local b = self:GCP()
		if b ~= self.S.ct then
			self.S.ct = b;
			self.S.tv = b and h(b).AssemblyLinearVelocity or Vector3.zero
		end;
		if self.S.ct then
			local b = h(self.S.ct)
			if b then
				local b = self:CP(b, a)
				self:HV(b)
				self:AA(b, a)
				return
			end
		end;
		self:HV(nil)
	end)
end;
function g:TA()
	self.S.e = not self.S.e;
	self.S.ct = nil;
	self.S.ifl = self.S.e;
	self.S.tv = Vector3.zero;
	if self.S.e then
		self:CA()
	else
		self:DA()
		self:HV(nil)
	end;
	return self.S.e
end;
function g:TMode()
	self.S.m = (self.S.m == "CAM") and "HRP" or "CAM";
	self.S.ifl = self.S.e;
	if self.S.m == "HRP" then
		self:HV(nil)
	end;
	return self.S.m
end;
return g

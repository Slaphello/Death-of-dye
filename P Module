local a = {
	Landscape = {
		Smoothness = 0.5,
		Prediction = getgenv().Aimlock_Prediction or 0.36,
		ShowDot = getgenv().Aimlock_ShowDot or true
	},
	flona = {
		IsGayLand = false,
		LockMode = "CAM",
		MultiTarget = false,
		Target = nil,
		IsGay = false,
		Velocity = {},
		AveragePing = 0,
		LastPredPos = nil
	}
}
local b = {}
local c = game:GetService("Players")
local d = game:GetService("RunService")
local e = game:GetService("Workspace")
local f = game:GetService("Stats")
local c = c.LocalPlayer;
local g = e.CurrentCamera;
local h = e:FindFirstChild("GameAssets")
local h = h and h:FindFirstChild("Teams")
task.spawn(function()
	local b = {}
	while task.wait(5) do
		local c = f.Network.ServerStatsItem["Data Ping"]:GetValue() / 3000;
		table.insert(b, c)
		if # b > 20 then
			table.remove(b, 1)
		end;
		local c = 0;
		for a, a in ipairs(b) do
			c = c + a
		end;
		a.flona.AveragePing = math.clamp(c / # b, 0.01, 0.1)
	end
end)
local function f(a)
	if not a or not a.Parent then
		return nil, nil
	end;
	local b = a:FindFirstChildOfClass("Humanoid")
	local a = a:FindFirstChild("HumanoidRootPart")
	if b and b.Health > 0 and a then
		return a, b
	end;
	return nil, nil
end;
local function i(a)
	local c = c.Character;
	if not c then
		return
	end;
	for c, c in ipairs(c:GetDescendants()) do
		if c:IsA("BasePart") and c.Name ~= "HumanoidRootPart" then
			if a then
				if b[c] == nil then
					b[c] = c.Transparency
				end;
				c.Transparency = math.max(b[c], a)
			else
				if b[c] ~= nil then
					c.Transparency = b[c]
				end
			end
		end
	end;
	if not a then
		table.clear(b)
	end
end;
local function j(a)
	if not h or not a then
		return nil
	end;
	local b = h:FindFirstChild("Killer")
	local c = h:FindFirstChild("Survivor")
	local d = h:FindFirstChild("Other")
	local e = h:FindFirstChild("Ghost")
	local f = b and # b:GetChildren() > 0;
	if f then
		if (d and a:IsDescendantOf(d)) or (e and a:IsDescendantOf(e)) or (c and a:IsDescendantOf(c)) then
			return b
		elseif b and a:IsDescendantOf(b) then
			return c
		end
	else
		if c and a:IsDescendantOf(c) then
			return c
		end;
		if d and a:IsDescendantOf(d) then
			return d
		end;
		if e and a:IsDescendantOf(e) then
			return e
		end
	end;
	for b, b in ipairs(h:GetChildren()) do
		if a:IsDescendantOf(b) then
			return b
		end
	end;
	return nil
end;
local function k()
	local a = f(c.Character)
	if not a or not h then
		return nil
	end;
	local b = j(c.Character)
	if not b then
		return nil
	end;
	local d = nil;
	local e = math.huge;
	local a = a.Position;
	for b, b in ipairs(b:GetChildren()) do
		if b ~= c.Character then
			local c = f(b)
			if c then
				local a = (c.Position - a).Magnitude;
				if a < e then
					e = a;
					d = b
				end
			end
		end
	end;
	return d
end;
local h;
local function j()
	if not h or not h.Parent then
		h = Instance.new("Part")
		h.Name = "_AimLock_DebugDot";
		h.Shape = Enum.PartType.Ball;
		h.Size = Vector3.new(3, 6, 3)
		h.Color = Color3.fromRGB(255, 50, 50)
		h.Material = Enum.Material.Neon;
		h.Anchored = true;
		h.CanCollide = false;
		h.CastShadow = false;
		h.Parent = e
	end;
	return h
end;
local function l()
	table.clear(a.flona.Velocity)
	a.flona.LastPredPos = nil
end;
local function m(b, c)
	if not b then
		return Vector3.zero
	end;
	local d = os.clock()
	local e = b.Position;
	local f = a.flona.Velocity[b];
	if not f then
		a.flona.Velocity[b] = {
			LastPos = e,
			LastTime = d,
			Velocity = Vector3.zero
		}
		return Vector3.zero
	end;
	local a = d - f.LastTime;
	if a > 0.001 then
		local a = (e - f.LastPos) / a;
		local b = math.clamp(c * 0.09, 0, 1)
		f.Velocity = f.Velocity:Lerp(a, b)
		f.LastPos = e;
		f.LastTime = d
	end;
	return f.Velocity
end;
local function n(b, d)
	if not b then
		return Vector3.zero
	end;
	local e = b.Position;
	local b = m(b, d)
	local a = a.Landscape.Prediction + a.flona.AveragePing;
	local c = f(c.Character)
	if c then
		local b = (e - c.Position).Magnitude;
		if b < 7 then
			local b = (7 - b) / 0.1 * 0.00284;
			a = math.max(0, a - b)
		end
	end;
	return e + (b * a)
end;
local function m(b, c, d)
	local e = g.CFrame.Position;
	if a.flona.LockMode == "CAM" then
		local b = 0.095;
		local b = 1 - ((1 - b) ^ (d * 60))
		if (c - e).Magnitude > 0.001 then
			local c = CFrame.lookAt(e, c)
			g.CFrame = a.flona.IsGay and c or g.CFrame:Lerp(c, b)
		end
	else
		local a = b.Position;
		local c = Vector3.new(c.X, a.Y, c.Z)
		if (c - a).Magnitude > 0.001 then
			b.CFrame = CFrame.lookAt(a, c)
		end
	end;
	a.flona.IsGay = false
end;
c.CharacterAdded:Connect(function()
	table.clear(b)
end)
e:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
	g = e.CurrentCamera
end)
d.RenderStepped:Connect(function(b)
	if a.flona.IsGayLand and a.flona.LockMode == "CAM" then
		i(0.85)
	else
		i(nil)
	end;
	if not a.flona.IsGayLand then
		a.flona.LastPredPos = nil;
		if h and h.Transparency < 1 then
			h.Transparency = 1
		end;
		return
	end;
	local c = f(c.Character)
	if not c then
		a.flona.LastPredPos = nil;
		return
	end;
	if a.flona.Target then
		local b, c = f(a.flona.Target)
		if not b or not c or c.Health <= 0 then
			a:ToggleAim()
			return
		end
	end;
	if a.flona.MultiTarget or not a.flona.Target then
		local b = k()
		if b ~= a.flona.Target then
			a.flona.Target = b;
			l()
		end
	end;
	if a.flona.Target then
		local d = f(a.flona.Target)
		if d then
			local d = n(d, b)
			a.flona.LastPredPos = d;
			if a.Landscape.ShowDot then
				local a = j()
				a.Position = d;
				a.Transparency = 0.3
			end;
			m(c, d, b)
			return
		end
	end;
	a.flona.LastPredPos = nil;
	if h and h.Transparency < 1 then
		h.Transparency = 1
	end
end)
function a:ToggleAim()
	self.flona.IsGayLand = not self.flona.IsGayLand;
	self.flona.Target = nil;
	l()
	self.flona.IsGay = self.flona.IsGayLand;
	if not self.flona.IsGayLand then
		i(nil)
		if h then
			h.Transparency = 1
		end
	end;
	return self.flona.IsGayLand
end;
function a:ToggleMulti()
	self.flona.MultiTarget = not self.flona.MultiTarget;
	self.flona.Target = nil;
	l()
	return self.flona.MultiTarget
end;
function a:ToggleMode()
	self.flona.LockMode = self.flona.LockMode == "CAM" and "HRP" or "CAM";
	self.flona.IsGay = self.flona.IsGayLand;
	if self.flona.LockMode == "HRP" then
		i(nil)
	end;
	return self.flona.LockMode
end;
return a

local a = {
	Settings = {
		Smoothness = 0.5,
		Prediction = getgenv().Aimlock_Prediction or 0.36,
		ShowDot = getgenv().Aimlock_ShowDot or true
	},
	State = {
		IsGayLand = false,
		LockMode = "CAM",
		MultiTarget = false,
		Target = nil,
		IsGay = false,
		Velocity = {},
		AveragePing = 0,
		LastPredPos = nil
	}
}
local b = {}
local c = game:GetService("Players")
local d = game:GetService("RunService")
local e = game:GetService("Workspace")
local f = game:GetService("Stats")
local g = game:GetService("ReplicatedStorage")
local c = c.LocalPlayer;
local g = e.CurrentCamera;
local h = e:FindFirstChild("GameAssets")
local h = h and h:FindFirstChild("Teams")
task.spawn(function()
	local b = {}
	while task.wait(5) do
		local c = f.Network.ServerStatsItem["Data Ping"]:GetValue() / 3000;
		table.insert(b, c)
		if # b > 20 then
			table.remove(b, 1)
		end;
		local c = 0;
		for a, a in ipairs(b) do
			c = c + a
		end;
		a.State.AveragePing = math.clamp(c / # b, 0.01, 0.1)
	end
end)
local function f(a)
	if not a or not a.Parent then
		return nil, nil
	end;
	local b = a:FindFirstChildOfClass("Humanoid")
	local a = a:FindFirstChild("HumanoidRootPart")
	if b and b.Health > 0 and a then
		return a, b
	end;
	return nil, nil
end;
local function i(a)
	local c = c.Character;
	if not c then
		return
	end;
	for c, c in ipairs(c:GetDescendants()) do
		if c:IsA("BasePart") and c.Name ~= "HumanoidRootPart" then
			if a then
				if b[c] == nil then
					b[c] = c.Transparency
				end;
				local b = b[c];
				c.Transparency = math.max(b, a)
			else
				if b[c] ~= nil then
					c.Transparency = b[c]
				end
			end
		end
	end;
	if not a then
		table.clear(b)
	end
end;
local function j(a)
	if not h or not a then
		return nil
	end;
	local b = h:FindFirstChild("Killer")
	local c = h:FindFirstChild("Survivor")
	local d = h:FindFirstChild("Other")
	local e = h:FindFirstChild("Ghost")
	local f = b and # b:GetChildren() > 0;
	if f then
		if (d and a:IsDescendantOf(d)) or (e and a:IsDescendantOf(e)) or (c and a:IsDescendantOf(c)) then
			return b
		elseif b and a:IsDescendantOf(b) then
			return c
		end
	else
		if c and a:IsDescendantOf(c) then
			return c
		end;
		if d and a:IsDescendantOf(d) then
			return d
		end;
		if e and a:IsDescendantOf(e) then
			return e
		end
	end;
	for b, b in ipairs(h:GetChildren()) do
		if a:IsDescendantOf(b) then
			return b
		end
	end;
	return nil
end;
local function k()
	local a = f(c.Character)
	if not a or not h then
		return nil
	end;
	local b = j(c.Character)
	if not b then
		return nil
	end;
	local d = nil;
	local e = math.huge;
	local a = a.Position;
	for b, b in ipairs(b:GetChildren()) do
		if b ~= c.Character then
			local c = f(b)
			if c then
				local a = (c.Position - a).Magnitude;
				if a < e then
					e = a;
					d = b
				end
			end
		end
	end;
	return d
end;
local h;
local function j()
	if not h or not h.Parent then
		h = Instance.new("Part")
		h.Name = "_AimLock_DebugDot";
		h.Shape = Enum.PartType.Ball;
		h.Size = Vector3.new(3, 6, 3)
		h.Color = Color3.fromRGB(255, 50, 50)
		h.Material = Enum.Material.Neon;
		h.Anchored = true;
		h.CanCollide = false;
		h.CastShadow = false;
		h.Parent = e
	end;
	return h
end;
local function l()
	table.clear(a.State.Velocity)
	a.State.LastPredPos = nil
end;
local function m(b, c)
	if not b then
		return Vector3.zero
	end;
	local d = os.clock()
	local e = b.Position;
	local f = a.State.Velocity[b];
	if not f then
		a.State.Velocity[b] = {
			LastPos = e,
			LastTime = d,
			Velocity = Vector3.zero
		}
		return Vector3.zero
	end;
	local a = d - f.LastTime;
	if a > 0.001 then
		local a = (e - f.LastPos) / a;
		local b = math.clamp(c * 10, 0, 1)
		f.Velocity = f.Velocity:Lerp(a, b)
		f.LastPos = e;
		f.LastTime = d
	end;
	return f.Velocity
end;
local function n(b, d)
	if not b then
		return Vector3.zero
	end;
	local e = b.Position;
	local b = m(b, d)
	local a = a.Settings.Prediction + a.State.AveragePing;
	local c = f(c.Character)
	if c then
		local b = (e - c.Position).Magnitude;
		if b < 7 then
			local b = (7 - b) / 0.1 * 0.00284;
			a = math.max(0, a - b)
		end
	end;
	return e + b * a
end;
local function m(b, c, d)
	local e = g.CFrame.Position;
	if a.State.LockMode == "CAM" then
		local b = math.clamp(a.Settings.Smoothness, 0.01, 0.99)
		local b = 1 - (1 - b) ^ (d * 60)
		if (c - e).Magnitude > 0.001 then
			local c = CFrame.lookAt(e, c)
			g.CFrame = a.State.IsGay and c or g.CFrame:Lerp(c, b)
		end
	else
		local a = b.Position;
		local c = Vector3.new(c.X, a.Y, c.Z)
		if (c - a).Magnitude > 0.001 then
			b.CFrame = CFrame.lookAt(a, c)
		end
	end;
	a.State.IsGay = false
end;
c.CharacterAdded:Connect(function()
	table.clear(b)
end)
d.RenderStepped:Connect(function(b)
	if a.State.IsGayLand and a.State.LockMode == "CAM" then
		i(0.85)
	else
		i(nil)
	end;
	if not a.State.IsGayLand then
		a.State.LastPredPos = nil;
		if h and h.Transparency < 1 then
			h.Transparency = 1
		end;
		return
	end;
	local c = c.Character;
	local c = f(c)
	if not c then
		a.State.LastPredPos = nil;
		return
	end;
	if a.State.Target then
		local b, c = f(a.State.Target)
		if not b or not c or c.Health <= 0 then
			a:ToggleAim()
			return
		end
	end;
	if a.State.MultiTarget or not a.State.Target then
		local b = k()
		if b ~= a.State.Target then
			a.State.Target = b;
			l()
		end
	end;
	if a.State.Target then
		local d = f(a.State.Target)
		if d then
			local d = n(d, b)
			a.State.LastPredPos = d;
			if a.Settings.ShowDot then
				local a = j()
				a.Position = d;
				a.Transparency = 0.3
			end;
			m(c, d, b)
			return
		end
	end;
	a.State.LastPredPos = nil;
	if h and h.Transparency < 1 then
		h.Transparency = 1
	end
end)
e:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
	g = e.CurrentCamera
end)
function a:ToggleAim()
	self.State.IsGayLand = not self.State.IsGayLand;
	self.State.Target = nil;
	l()
	self.State.IsGay = self.State.IsGayLand;
	if not self.State.IsGayLand then
		i(nil)
		if h then
			h.Transparency = 1
		end
	end;
	return self.State.IsGayLand
end;
function a:ToggleMulti()
	self.State.MultiTarget = not self.State.MultiTarget;
	self.State.Target = nil;
	l()
	return self.State.MultiTarget
end;
function a:ToggleMode()
	self.State.LockMode = self.State.LockMode == "CAM" and "HRP" or "CAM";
	self.State.IsGay = self.State.IsGayLand;
	if self.State.LockMode == "HRP" then
		i(nil)
	end;
	return self.State.LockMode
end;
local b;
b = hookmetamethod(game, "__namecall", function(c, ...)
	local d = getnamecallmethod()
	local e = {
		...
	}
	if d == "FireServer" and c.Name == "UpdateServerPos" then
		if a.State.IsGayLand and a.State.LockMode == "HRP" and a.State.LastPredPos then
			local b = e[1];
			if typeof(b) == "CFrame" then
				local b = b.Position;
				local a = Vector3.new(a.State.LastPredPos.X, b.Y, a.State.LastPredPos.Z)
				e[1] = CFrame.lookAt(b, a)
			end
		end
	end;
	return b(c, unpack(e))
end)
return a

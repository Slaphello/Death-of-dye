local a = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local b = {
	["Survivor"] = true,
	["Killer"] = true,
	["Other"] = true
}
local c = Color3.fromRGB(0, 255, 0)
local d = Color3.fromRGB(255, 0, 0)
local e = Color3.fromRGB(255, 255, 0)
local function f(a, b)
	if not a:IsA("Model") then
		return
	end;
	for a, a in ipairs(a:GetChildren()) do
		if a:IsA("Highlight") then
			a:Destroy()
		end
	end;
	local f = Instance.new("Highlight")
	f.Name = "TMHighlight";
	f.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop;
	f.Parent = a;
	if b == "Other" then
		f.FillColor = e;
		f.OutlineColor = e;
		f.FillTransparency = 0.9;
		return
	end;
	local e = a:WaitForChild("Head")
	local g = a:WaitForChild("Humanoid")
	local h, i;
	if e and g then
		if e:FindFirstChild("StatsUI") then
			e.StatsUI:Destroy()
		end;
		local a = Instance.new("BillboardGui")
		a.Name = "StatsUI";
		a.Adornee = e;
		a.Size = UDim2.new(9.5, 0, 3.5, 0)
		a.StudsOffset = Vector3.new(0, 4, 0)
		a.AlwaysOnTop = true;
		a.Parent = e;
		h = Instance.new("TextLabel")
		h.Size = UDim2.new(1, 0, 0.5, 0)
		h.Position = UDim2.new(0, 0, 0, 0)
		h.BackgroundTransparency = 1;
		h.TextColor3 = Color3.fromRGB(85, 255, 127)
		h.TextStrokeTransparency = 0;
		h.TextScaled = true;
		h.Font = Enum.Font.SourceSansBold;
		h.Parent = a;
		i = h:Clone()
		i.Position = UDim2.new(0, 0, 0.5, 0)
		i.TextColor3 = Color3.fromRGB(255, 220, 85)
		i.Parent = a
	end;
	local b = b == "Survivor" and c or d;
	local function c()
		local c = a:GetAttribute("UsingAbility") == true;
		local b = c and Color3.fromRGB(0, 170, 255) or b;
		f.FillColor = b;
		f.OutlineColor = b;
		f.FillTransparency = c and 0.6 or 0.75;
		if g and h and i then
			h.Text = string.format("HP: %d / %d", g.Health, g.MaxHealth)
			i.Text = string.format("Stamina: %d", a:GetAttribute("StaminaServer") or 0)
		end
	end;
	c()
	local b = {
		a.ChildAdded:Connect(function(a)
			if a:IsA("Highlight") and a.Name ~= "TMHighlight" and a.Name ~= "HoverHighlight" then
				task.defer(function()
					if a.Parent then
						a:Destroy()
					end
				end)
			end
		end),
		a:GetAttributeChangedSignal("UsingAbility"):Connect(c),
		a:GetAttributeChangedSignal("StaminaServer"):Connect(c)
	}
	table.insert(b, g:GetPropertyChangedSignal("Health"):Connect(c))
	table.insert(b, g:GetPropertyChangedSignal("MaxHealth"):Connect(c))
	table.insert(b, a.AncestryChanged:Connect(function(a, a)
		if not a then
			for a, a in ipairs(b) do
				a:Disconnect()
			end;
			table.clear(b)
		end
	end))
end;
for b, c in pairs(b) do
	local a = a:FindFirstChild(b) or a:WaitForChild(b, 5)
	if a then
		for a, a in ipairs(a:GetChildren()) do
			f(a, b)
		end;
		a.ChildAdded:Connect(function(a)
			f(a, b)
		end)
	end
end

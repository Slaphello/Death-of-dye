local a = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local function b(a, b)
	if not a:IsA("Model") then
		return
	end;
	for a, a in ipairs(a:GetChildren()) do
		if a:IsA("Highlight") then
			a:Destroy()
		end
	end;
	local c = Instance.new("Highlight")
	c.Name = "TMHighlight";
	c.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop;
	c.Parent = a;
	local d = {}
	local e = a.ChildAdded:Connect(function(a)
		if a:IsA("Highlight") then
			if a.Name ~= "TMHighlight" and a.Name ~= "HoverHighlight" then
				task.defer(function()
					if a and a.Parent then
						a:Destroy()
					end
				end)
			end
		end
	end)
	table.insert(d, e)
	local e = (b == "Survivor") and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
	local f = Color3.fromRGB(0, 170, 255)
	local function g()
		if b == "Other" then
			c.FillColor = f;
			c.OutlineColor = f;
			c.FillTransparency = 0.85
		else
			local a = (a:GetAttribute("UsingAbility") == true)
			local b = a and f or e;
			c.FillColor = b;
			c.OutlineColor = b;
			c.FillTransparency = a and 0.65 or 0.75
		end
	end;
	g()
	local b = a:GetAttributeChangedSignal("UsingAbility"):Connect(g)
	table.insert(d, b)
	local a = a.AncestryChanged:Connect(function(a, a)
		if not a then
			for a, a in ipairs(d) do
				a:Disconnect()
			end;
			table.clear(d)
		end
	end)
	table.insert(d, a)
end;
local c = {
	"Survivor",
	"Killer",
	"Other"
}
for c, c in ipairs(c) do
	local a = a:WaitForChild(c)
	for a, a in ipairs(a:GetChildren()) do
		b(a, c)
	end;
	a.ChildAdded:Connect(function(a)
		b(a, c)
	end)
end

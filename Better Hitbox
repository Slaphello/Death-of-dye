local a = game:GetService("Players")
local b = game:GetService("ReplicatedStorage")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer;
local f = e:WaitForChild("PlayerScripts")
if f then
	local a = f:FindFirstChild("ClientReplicator")
	if a then
		a:Destroy()
	end
end;
local f = 0;
task.spawn(function()
	local a = {}
	local b = 60;
	local c = d.Network.ServerStatsItem["Data Ping"];
	while task.wait(0.5) do
		local c = c:GetValue() / 1850;
		table.insert(a, c)
		if # a > b then
			table.remove(a, 1)
		end;
		local b = 0;
		for a, a in ipairs(a) do
			b = b + a
		end;
		local a = b / # a;
		f = math.clamp(a, 0.02, 0.1)
	end
end)
local b = b:WaitForChild("Events"):WaitForChild("RemoteEvents")
local b = b:WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local d = workspace:WaitForChild("GameAssets")
local d = d:WaitForChild("Teams")
repeat
	task.wait(0.5)
until e:FindFirstChild("Stats") and e.Stats:FindFirstChild("LoadedIn")
local g = 0;
local h = 0.034;
c.RenderStepped:Connect(function(c)
	g = g + c;
	local i = e.Character;
	if i and i:IsDescendantOf(d) then
		if g >= h then
			g = 0;
			local c = i:FindFirstChild("HumanoidRootPart")
			if c then
				local e = i:FindFirstChild("PosValue")
				if e and e.Value then
					c:PivotTo(e.Value.CFrame)
				end;
				local e = c.CFrame;
				local g = i:GetAttribute("Ragdolled")
				local h = g and Vector3.zero or c.AssemblyLinearVelocity;
				h = h * f;
				if h.Magnitude > 30 then
					h = h.Unit * 30
				end;
				local c = g and Vector3.zero or c.AssemblyAngularVelocity;
				local g = c.Magnitude;
				local j = CFrame.identity;
				if g > 0.001 then
					local a = c.Unit;
					local b = g * f;
					j = CFrame.fromAxisAngle(a, b)
				end;
				local c = (e + h) * j;
				local e = i:GetAttribute("Stamina") or 0;
				b:FireServer(c, e, workspace:GetServerTimeNow())
				local b = {
					"Survivor",
					"Killer",
					"Other"
				}
				for b, b in ipairs(b) do
					local b = d:FindFirstChild(b)
					if b then
						for b, b in ipairs(b:GetChildren()) do
							if not a:GetPlayerFromCharacter(b) then
								local a = b:FindFirstChild("PosValue")
								if a and b.PrimaryPart then
									b.PrimaryPart:PivotTo(a.Value.CFrame)
								end
							end
						end
					end
				end
			end
		end
	end;
	local b = math.clamp(0.12 + f, 0.01, 0.99)
	local b = 1 - math.pow(1 - b, c * 60)
	b = math.clamp(b, 0, 1)
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= e then
			local a = a.Character;
			if a and not a:FindFirstChild("PosValue") then
				local c = a:FindFirstChild("HumanoidRootPart")
				if c then
					local a = a:FindFirstChild("Replicator_Good")
					local a = a and a.CFrame or c.CFrame;
					c.CFrame = c.CFrame:Lerp(a, b)
				end
			end
		end
	end
end)

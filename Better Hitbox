local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer
local g = ipairs
local h = pairs
local i = math.clamp
local j = math.min
local k = math.max
local l = math.floor
local m = task.wait
local n = task.spawn
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end
local o = {
	p = 99,
	q = 10,
	r = 2,
	s = 0.14,
	t = 0.24,
	u = 0.001,
	v = 0.0002
}
local w = {
	x = 50,
	y = 0.035,
	z = table.create(o.q, 0),
	aa = 1,
	ab = {}
}
local ac = Vector3.new(0, - 8, 0)
local ad = Vector3.new(0, 0.5, 0)
local ae = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local af = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local ag = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if ag then
	ag:Destroy()
end
local ah = {}
local ai = RaycastParams.new()
ai.FilterType = Enum.RaycastFilterType.Exclude
ai.IgnoreWater = true
ai.RespectCanCollide = true
local function aj()
	ai.FilterDescendantsInstances = ah
end
local function ak(al)
	if al.Name == "HumanoidRootPart" and al:IsA("BasePart") then
		local am = al.Parent
		if am and am:IsA("Model") and not table.find(ah, am) then
			table.insert(ah, am)
			aj()
		end
	end
end
local function an(ao)
	if ao.Name == "HumanoidRootPart" and ao:IsA("BasePart") then
		local ap = ao.Parent
		if ap then
			local aq = table.find(ah, ap)
			if aq then
				table.remove(ah, aq)
				aj()
			end
		end
	end
end
for ar, as in g(ae:GetChildren()) do
	for at, au in g(as:GetDescendants()) do
		ak(au)
	end
end
ae.DescendantAdded:Connect(ak)
ae.DescendantRemoving:Connect(an)
local function av(aw)
	if w.ab[aw] and w.ab[aw].ax then
		w.ab[aw].ax:Disconnect()
	end
	w.ab[aw] = nil
end
local function ay(az, ba)
	if az == f or not ba or not ba.Parent then
		return
	end
	local bb = ba.Parent.Name
	if bb ~= "Survivor" and bb ~= "Killer" then
		return
	end
	local bc = ba:WaitForChild("HumanoidRootPart", 5)
	local bd = ba:WaitForChild("Humanoid", 5)
	if not bc or not bd then
		return
	end
	av(az)
	local be = bd:WaitForChild("Animator", 5)
	local bf = nil
	if be then
		bf = be.AnimationPlayed:Connect(function(bg)
			n(function()
				m()
				if bg and bg.IsPlaying then
					local bh = bg.Length
					if bh > 0 then
						bg.TimePosition = j(bg.TimePosition + 0.08, bh)
					else
						bg.TimePosition = bg.TimePosition + 0.08
					end
				end
			end)
		end)
	end
	local bi = {
		bj = ba,
		bk = bc,
		bl = bd,
		bm = ba:FindFirstChild("Replicator_Good"),
		bn = o.s,
		ax = bf
	}
	w.ab[az] = bi
end
local function bo(bp)
	if bp == f then
		return
	end
	if bp.Character then
		n(ay, bp, bp.Character)
	end
	bp.CharacterAdded:Connect(function(bq)
		n(ay, bp, bq)
	end)
	bp.CharacterRemoving:Connect(function()
		av(bp)
	end)
end
for br, bs in g(a:GetPlayers()) do
	bo(bs)
end
a.PlayerAdded:Connect(bo)
a.PlayerRemoving:Connect(av)
n(function()
	local bt = nil
	while m(o.r) do
		if not bt then
			local bu = c:FindFirstChild("Network")
			local bv = bu and bu:FindFirstChild("ServerStatsItem")
			bt = bv and bv:FindFirstChild("Data Ping")
		end
		if bt then
			local bw = bt:GetValue()
			if typeof(bw) == "number" then
				w.z[w.aa] = bw
				w.aa = w.aa % o.q + 1
				local bx = 0
				for by = 1, o.q do
					bx += w.z[by]
				end
				local bz = bx / o.q
				w.x = bz
				local ca = f.Character
				local cb = (ca and ca.Parent and ca.Parent.Name == "Killer")
				local cc = getgenv().AccurateHurtbox and (cb and 800 or 700) or (cb and 900 or 800)
				w.y = i(bz / cc, 0.04, 0.15)
			end
		end
	end
end)
b.PreSimulation:Connect(function(cd)
	local ce = cd * 60
	local cf = f.Character
	if cf and cf.Parent then
		local cg = cf:FindFirstChild("HumanoidRootPart")
		local ch = cf:FindFirstChildOfClass("Humanoid")
		if cg and ch and cg:IsDescendantOf(ae) then
			local ci = cg.CFrame
			local cj = ci.Position
			local ck = cg.AssemblyLinearVelocity * w.y
			if ck.Magnitude > o.p then
				ck = ck.Unit * o.p
			end
			local cl = cj + ck
			local cm = cl - cj
			if cm.Magnitude > 0.01 then
				local cn = e:Raycast(cj, cm, ai)
				if cn then
					cl = cn.Position + (cn.Normal * 0.2)
				end
			end
			local co = e:Raycast(cl + ad, ac, ai)
			if co then
				local cp = co.Position.Y + ch.HipHeight + (cg.Size.Y * 0.5)
				if cl.Y < cp then
					cl = Vector3.new(cl.X, cp, cl.Z)
				end
			end
			local cq = ci - cj + cl
			local cr = l((cf:GetAttribute("Stamina") or 0) * 10) / 10
			af:FireServer(cq, cr, e:GetServerTimeNow())
		end
	end
	for cs, ct in h(w.ab) do
		local cu = ct.bj
		local cv = ct.bk
		local cw = ct.bm
		if not cs.Parent or not cu or not cu.Parent or not cv or not cv.Parent then
			av(cs)
			continue
		end
		if not cw or not cw.Parent then
			cw = cu:FindFirstChild("Replicator_Good")
			if cw then
				ct.bm = cw
			else
				continue
			end
		end
		local cx = (cv.Position - cw.Position).Magnitude
		local cy = i(cx / 8.35, o.s, o.t)
		local cz = ct.bn
		if cy > cz then
			cz = j(cz + o.u, cy)
		elseif cy < cz then
			cz = k(cz - o.v, cy)
		end
		cz = i(cz, o.s, o.t)
		ct.bn = cz
		local da = 1 - (1 - cz) ^ ce
		cv.CFrame = cv.CFrame:Lerp(cw.CFrame, da)
	end
end)

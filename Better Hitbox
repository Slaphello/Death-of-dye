local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer;
local g = ipairs;
local h = pairs;
local i = math.clamp;
local j = math.min;
local k = math.max;
local l = math.floor;
local m = task.wait;
local n = task.spawn;
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end;
local o = {
	M_VLO = 99,
	AvgPing = 60,
	PUI = 2,
	NnL = 0.14,
	MxL = 0.24,
	LPS = 0.001,
	LPF = 0.0002
}
local p = {
	PTime = 0.035,
	PH = table.create(o.AvgPing, 0),
	PIdex = 1,
	TdP = {}
}
local q = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local d = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local r = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if r then
	r:Destroy()
end;
n(function()
	local a;
	while m(o.PUI) do
		if not a then
			local b = c:FindFirstChild("Network")
			local b = b and b:FindFirstChild("ServerStatsItem")
			a = b and b:FindFirstChild("Data Ping")
		end;
		if not a then
			continue
		end;
		local a = a:GetValue()
		if typeof(a) ~= "number" then
			continue
		end;
		p.PH[p.PIdex] = a;
		p.PIdex = p.PIdex % o.AvgPing + 1;
		local a = 0;
		for b = 1, o.AvgPing do
			a += p.PH[b]
		end;
		local a = a / o.AvgPing;
		local b = getgenv().AccurateHurtbox == true and 800 or 1400;
		p.PTime = i(a / b, 0.035, 0.165)
	end
end)
local function c(a, b)
	if a == f or not b or not b.Parent then
		return
	end;
	local c = b.Parent;
	if c.Name ~= "Survivor" and c.Name ~= "Killer" then
		return
	end;
	local c = b:WaitForChild("HumanoidRootPart", 5)
	if not c then
		return
	end;
	p.TdP[a] = {
		Character = b,
		HRP = c,
		Repli = b:FindFirstChild("Replicator_Good"),
		Lerp = o.NnL
	}
end;
local function m(a)
	if a == f then
		return
	end;
	if a.Character then
		n(c, a, a.Character)
	end;
	a.CharacterAdded:Connect(function(b)
		n(c, a, b)
	end)
	a.CharacterRemoving:Connect(function()
		p.TdP[a] = nil
	end)
end;
for a, a in g(a:GetPlayers()) do
	m(a)
end;
a.PlayerAdded:Connect(m)
a.PlayerRemoving:Connect(function(a)
	p.TdP[a] = nil
end)
b.RenderStepped:Connect(function(a)
	local a = a * 60;
	local b = f.Character;
	if b and b.Parent then
		local a = b:FindFirstChild("HumanoidRootPart")
		if a and a:IsDescendantOf(q) then
			local c = a.AssemblyLinearVelocity * p.PTime;
			if c.Magnitude > o.M_VLO then
				c = c.Unit * o.M_VLO
			end;
			local b = b:GetAttribute("Stamina") or 0;
			b = l(b * 1000) / 1000;
			d:FireServer(a.CFrame + c, b, e:GetServerTimeNow())
		end
	end;
	for b, c in h(p.TdP) do
		local d = c.Character;
		local e = c.HRP;
		local f = c.Repli;
		if not b.Parent or not d or not d.Parent or not e or not e.Parent then
			p.TdP[b] = nil;
			continue
		end;
		if not f or not f.Parent then
			f = d:FindFirstChild("Replicator_Good")
			if f then
				c.Repli = f
			else
				continue
			end
		end;
		local b = (e.Position - f.Position).Magnitude;
		local b = i(b / 8.35, o.NnL, o.MxL)
		local d = c.Lerp;
		if b > d then
			d = j(d + o.LPS, b)
		elseif b < d then
			d = k(d - o.LPF, b)
		end;
		d = i(d, o.NnL, o.MxL)
		c.Lerp = d;
		local a = 1 - (1 - d) ^ a;
		e.CFrame = e.CFrame:Lerp(f.CFrame, a)
	end
end)

local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer
local g = "rbxassetid://125600990923275"
local h = "rbxassetid://127982599288788"
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end
local i = {
	j = 99,
	k = 10,
	l = 2,
	m = 0.14,
	n = 0.24,
	o = 0.001,
	p = 0.0002
}
local q = {
	r = 40,
	s = 0.035,
	t = table.create(i.k, 0),
	u = 1,
	v = {}
}
local w = Vector3.new(0, - 8, 0)
local x = Vector3.new(0, 0.5, 0)
local y = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local z = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local aa = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if aa then
	aa:Destroy()
end
local ab = {}
local ac = RaycastParams.new()
ac.FilterType = Enum.RaycastFilterType.Exclude
ac.IgnoreWater = true
ac.RespectCanCollide = true
local function ad()
	ac.FilterDescendantsInstances = ab
end
local function ae(af)
	if af.Name == "HumanoidRootPart" and af:IsA("BasePart") then
		local ag = af.Parent
		if ag and ag:IsA("Model") and not table.find(ab, ag) then
			table.insert(ab, ag)
			ad()
		end
	end
end
local function ah(ai)
	if ai.Name == "HumanoidRootPart" and ai:IsA("BasePart") then
		local aj = ai.Parent
		if aj then
			local ak = table.find(ab, aj)
			if ak then
				table.remove(ab, ak)
				ad()
			end
		end
	end
end
for al, am in ipairs(y:GetChildren()) do
	for an, ao in ipairs(am:GetDescendants()) do
		ae(ao)
	end
end
y.DescendantAdded:Connect(ae)
y.DescendantRemoving:Connect(ah)
local function ap()
	local aq = c:FindFirstChild("Network")
	local ar = aq and aq:FindFirstChild("ServerStatsItem")
	local as = ar and ar:FindFirstChild("Data Ping")
	if as then
		local at = as:GetValue()
		if typeof(at) == "number" then
			return at
		end
	end
	return 0
end
local function au(av)
	local aw = av:WaitForChild("HumanoidRootPart")
	local ax = av:WaitForChild("Humanoid")
	local ay = ax:WaitForChild("Animator")
	local az = false
	local function ba(bb)
		for bc, bd in ipairs(aw:GetChildren()) do
			if bd:IsA("BodyVelocity") and bd ~= bb then
				bd:Destroy()
			end
		end
	end
	ay.AnimationPlayed:Connect(function(be)
		local bf = be.Animation
		if not bf then
			return
		end
		local bg = bf.AnimationId
		if bg ~= g and bg ~= h then
			return
		end
		if az then
			return
		end
		az = true
		ba()
		local bh = ap()
		local bi = bh / 1000
		if bg == g then
			local bj = math.max(0, 0.65 - bi)
			task.delay(bj, function()
				if not aw.Parent then
					az = false
					return
				end
				ba()
				local bk = aw.CFrame.LookVector
				local bl = 25 / 0.4
				local bm = Instance.new("BodyVelocity")
				bm.Name = "DashVelocity"
				bm.MaxForce = Vector3.new(math.huge, 0, math.huge)
				bm.P = math.huge
				bm.Velocity = bk * bl
				bm.Parent = aw
				local bn
				bn = b.Heartbeat:Connect(function()
					if not aw.Parent or not bm.Parent then
						bn:Disconnect()
						az = false
						return
					end
					ba(bm)
					bm.Velocity = bk * bl
				end)
				task.delay(0.4, function()
					if bn then
						bn:Disconnect()
					end
					if bm.Parent then
						bm:Destroy()
					end
					az = false
				end)
			end)
		elseif bg == h then
			task.delay(0.8, function()
				if not aw.Parent then
					az = false
					return
				end
				ba()
				local bo = aw.CFrame.LookVector
				local bp = 5 / 0.2
				local bq = Instance.new("BodyVelocity")
				bq.Name = "BiteVelocity"
				bq.MaxForce = Vector3.new(math.huge, 0, math.huge)
				bq.P = math.huge
				bq.Velocity = bo * bp
				bq.Parent = aw
				local br
				br = b.Heartbeat:Connect(function()
					if not aw.Parent or not bq.Parent then
						br:Disconnect()
						az = false
						return
					end
					ba(bq)
					bq.Velocity = bo * bp
				end)
				task.delay(0.2, function()
					if br then
						br:Disconnect()
					end
					if bq.Parent then
						bq:Destroy()
					end
					az = false
				end)
			end)
		end
	end)
end
if f.Character then
	au(f.Character)
end
f.CharacterAdded:Connect(au)
local function bs(bt)
	if q.v[bt] and q.v[bt].bu then
		q.v[bt].bu:Disconnect()
	end
	q.v[bt] = nil
end
local function bv(bw, bx)
	if bw == f or not bx or not bx.Parent then
		return
	end
	local by = bx.Parent.Name
	if by ~= "Survivor" and by ~= "Killer" then
		return
	end
	local bz = bx:WaitForChild("HumanoidRootPart", 5)
	local ca = bx:WaitForChild("Humanoid", 5)
	if not bz or not ca then
		return
	end
	bs(bw)
	local cb = ca:WaitForChild("Animator", 5)
	local cc = nil
	if cb then
		cc = cb.AnimationPlayed:Connect(function(cd)
			task.spawn(function()
				task.wait()
				if cd and cd.IsPlaying then
					local ce = cd.Length
					if ce > 0 then
						cd.TimePosition = math.min(cd.TimePosition + 0.04, ce)
					else
						cd.TimePosition = cd.TimePosition + 0.04
					end
				end
			end)
		end)
	end
	local cf = {
		cg = bx,
		ch = bz,
		ci = ca,
		cj = bx:FindFirstChild("Replicator_Good"),
		ck = i.m,
		bu = cc
	}
	q.v[bw] = cf
end
local function cl(cm)
	if cm == f then
		return
	end
	if cm.Character then
		task.spawn(bv, cm, cm.Character)
	end
	cm.CharacterAdded:Connect(function(cn)
		task.spawn(bv, cm, cn)
	end)
	cm.CharacterRemoving:Connect(function()
		bs(cm)
	end)
end
for co, cp in ipairs(a:GetPlayers()) do
	cl(cp)
end
a.PlayerAdded:Connect(cl)
a.PlayerRemoving:Connect(bs)
task.spawn(function()
	while task.wait(i.l) do
		local cq = ap()
		if typeof(cq) == "number" and cq > 0 then
			q.t[q.u] = cq
			q.u = q.u % i.k + 1
			local cr = 0
			for cs = 1, i.k do
				cr += q.t[cs]
			end
			local ct = cr / i.k
			q.r = ct
			local cu = f.Character
			local cv = (cu and cu.Parent and cu.Parent.Name == "Killer")
			local cw = getgenv().AccurateHurtbox and (cv and 800 or 700) or (cv and 900 or 800)
			q.s = math.clamp(ct / cw, 0.04, 0.15)
		end
	end
end)
b.PreSimulation:Connect(function(cx)
	local cy = cx * 60
	local cz = f.Character
	if cz and cz.Parent then
		local da = cz:FindFirstChild("HumanoidRootPart")
		local db = cz:FindFirstChildOfClass("Humanoid")
		if da and db and da:IsDescendantOf(y) then
			local dc = da.CFrame
			local dd = dc.Position
			local de = da.AssemblyLinearVelocity * q.s
			if de.Magnitude > i.j then
				de = de.Unit * i.j
			end
			local df = dd + de
			local dg = df - dd
			if dg.Magnitude > 0.01 then
				local dh = e:Raycast(dd, dg, ac)
				if dh then
					df = dh.Position + (dh.Normal * 0.2)
				end
			end
			local di = e:Raycast(df + x, w, ac)
			if di then
				local dj = di.Position.Y + db.HipHeight + (da.Size.Y * 0.5)
				if df.Y < dj then
					df = Vector3.new(df.X, dj, df.Z)
				end
			end
			local dk = dc - dd + df
			local dl = math.floor((cz:GetAttribute("Stamina") or 0) * 10) / 10
			z:FireServer(dk, dl, e:GetServerTimeNow())
		end
	end
	for dm, dn in pairs(q.v) do
		local do_ = dn.cg
		local dp = dn.ch
		local dq = dn.cj
		if not dm.Parent or not do_ or not do_.Parent or not dp or not dp.Parent then
			bs(dm)
			continue
		end
		if not dq or not dq.Parent then
			dq = do_:FindFirstChild("Replicator_Good")
			if dq then
				dn.cj = dq
			else
				continue
			end
		end
		local dr = (dp.Position - dq.Position).Magnitude
		local ds = math.clamp(dr / 8.35, i.m, i.n)
		local dt = dn.ck
		if ds > dt then
			dt = math.min(dt + i.o, ds)
		elseif ds < dt then
			dt = math.max(dt - i.p, ds)
		end
		dt = math.clamp(dt, i.m, i.n)
		dn.ck = dt
		local du = 1 - (1 - dt) ^ cy
		dp.CFrame = dp.CFrame:Lerp(dq.CFrame, du)
	end
end)

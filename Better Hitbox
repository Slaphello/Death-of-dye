local a = game:GetService("Players")
local b = a.LocalPlayer;
local c = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local d = game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
if b:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator") then
	b.PlayerScripts.ClientReplicator:Destroy()
end;
local e = {}
local f, g = 0, {}
task.spawn(function()
	while task.wait(5) do
		local a = game:GetService("Stats"):FindFirstChild("Network")
		local a = a and a:FindFirstChild("ServerStatsItem") and a.ServerStatsItem:FindFirstChild("Data Ping")
		if a and typeof(a:GetValue()) == "number" then
			table.insert(g, a:GetValue() / 1215)
			if # g > 20 then
				table.remove(g, 1)
			end;
			local a = 0;
			for b, b in ipairs(g) do
				a += b
			end;
			if # g > 0 then
				f = math.clamp(a / # g, 0.02, 0.1)
			end
		end
	end
end)
task.spawn(function()
	while task.wait(15) do
		local d = {}
		for a, a in ipairs(a:GetPlayers()) do
			if a ~= b and a.Character then
				local b = a.Character.Parent;
				if b and (b.Name == "Survivor" or b.Name == "Killer") and b:IsDescendantOf(c) then
					table.insert(d, a)
				end
			end
		end;
		if # d > 0 then
			local a = d[math.random(1, # d)];
			local b = e[a] or "N/A";
			print(string.format("[Debug] Average Ping: %.4f | Player: %s (%s) | Lerp: %s", f, a.Name, a.Character.Parent.Name, tostring(b)))
		else
			print(string.format("[Debug] Average Ping: %.4f | Game Not Start Yet", f))
		end
	end
end)
game:GetService("RunService").RenderStepped:Connect(function(g)
	local h = b.Character;
	if not h or not h:IsDescendantOf(c) then
		return
	end;
	if h:GetAttribute("Ragdolled") == true then
		return
	end;
	local i = h:FindFirstChild("HumanoidRootPart")
	if i then
		local b = i.AssemblyLinearVelocity * f;
		if b.Magnitude > 40 then
			b = b.Unit * 40
		end;
		d:FireServer(i.CFrame + b, h:GetAttribute("Stamina") or 0, workspace:GetServerTimeNow() - f)
		for b, b in ipairs(c:GetChildren()) do
			for b, b in ipairs(b:GetChildren()) do
				if b:IsA("Model") and not a:GetPlayerFromCharacter(b) then
					local a = b:FindFirstChild("PosValue")
					if a and a.Value then
						local a = a.Value;
						if typeof(a) == "CFrame" then
							b:PivotTo(a)
						elseif typeof(a) == "Instance" and a:IsDescendantOf(workspace) then
							if a:IsA("BasePart") then
								b:PivotTo(a.CFrame)
							elseif a:IsA("Model") then
								b:PivotTo(a:GetPivot())
							end
						end
					end
				end
			end
		end
	end;
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= b and a.Character and not a.Character:FindFirstChild("PosValue") then
			local b = a.Character:FindFirstChild("HumanoidRootPart")
			if b then
				local c = a.Character:FindFirstChild("Replicator_Good")
				local c = (c and c:IsA("BasePart")) and c.CFrame or b.CFrame;
				if c then
					local d = e[a] or 0.1;
					local f = math.clamp((b.Position - c.Position).Magnitude / 8.34, 0.1, 0.35)
					local d = math.clamp(f, d - 0.000101, d + 0.001)
					e[a] = d;
					local a = 1 - math.pow(1 - d, g * 60)
					b.CFrame = b.CFrame:Lerp(c, a)
				end
			end
		end
	end
end)
a.PlayerRemoving:Connect(function(a)
	e[a] = nil
end)

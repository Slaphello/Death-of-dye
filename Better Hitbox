local a = game:GetService("Players")
local b = game:GetService("ReplicatedStorage")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer;
local f = e:WaitForChild("PlayerScripts")
if f then
	local a = f:FindFirstChild("ClientReplicator")
	if a then
		a:Destroy()
	end
end;
local f = 0;
task.spawn(function()
	local a = {}
	local b = 60;
	while task.wait(0.5) do
		local c = d.Network.ServerStatsItem["Data Ping"]:GetValue() / 1850;
		table.insert(a, c)
		if # a > b then
			table.remove(a, 1)
		end;
		local b = 0;
		for a, a in ipairs(a) do
			b = b + a
		end;
		local a = b / # a;
		f = math.clamp(a, 0.02, 0.1)
	end
end)
local b = b:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local d = workspace:WaitForChild("GameAssets")
local d = d:WaitForChild("Teams")
repeat
	task.wait(0.5)
until e:FindFirstChild("Stats") and e.Stats:FindFirstChild("LoadedIn")
local g = 0;
c.RenderStepped:Connect(function(c)
	g = g + c;
	local c = e.Character;
	if not c or not c:IsDescendantOf(d) then
		return
	end;
	if g >= 0.034 then
		g = 0;
		local e = c:FindFirstChild("HumanoidRootPart")
		if e then
			local g = c:FindFirstChild("PosValue")
			if g and g.Value then
				e:PivotTo(g.Value.CFrame)
			end;
			local g = e.CFrame;
			local h = c:GetAttribute("Ragdolled")
			local i = h and Vector3.zero or e.AssemblyLinearVelocity;
			i = i * f;
			if i.Magnitude > 30 then
				i = i.Unit * 30
			end;
			local e = h and Vector3.zero or e.AssemblyAngularVelocity;
			local e = CFrame.Angles((e.X * f) * 0.65, (e.Y * f) * 0.1, (e.Z * f) * 0.65)
			local e = (g + i) * e;
			local c = c:GetAttribute("Stamina") or 0;
			b:FireServer(e, c, workspace:GetServerTimeNow())
			for b, b in ipairs({
				"Survivor",
				"Killer",
				"Other"
			}) do
				local b = d:FindFirstChild(b)
				if b then
					for b, b in ipairs(b:GetChildren()) do
						if not a:GetPlayerFromCharacter(b) then
							local a = b:FindFirstChild("PosValue")
							if a and b.PrimaryPart then
								b.PrimaryPart:PivotTo(a.Value.CFrame)
							end
						end
					end
				end
			end
		end
	end;
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= e then
			local a = a.Character;
			if a and not a:FindFirstChild("PosValue") then
				local b = a:FindFirstChild("HumanoidRootPart")
				if b then
					local a = a:FindFirstChild("Replicator_Good")
					local a = a and a.CFrame or b.CFrame;
					b.CFrame = b.CFrame:Lerp(a, 0.11 + f)
				end
			end
		end
	end
end)

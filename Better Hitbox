local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer
local g = ipairs
local h = pairs
local i = math.clamp
local j = math.floor
local k = task.wait
local l = task.spawn
local m = task.delay
local n = "rbxassetid://125600990923275"
local o = "rbxassetid://127982599288788"
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end
local p = {
	a = 99,
	b = 10,
	c = 2,
	d = 0.14,
	e = 0.24,
	f = 0.001,
	g = 0.0002
}
local q = {
	a = 40,
	b = 0.035,
	c = table.create(p.b, 0),
	d = 1,
	e = {}
}
local r = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local s = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local t = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if t then
	t:Destroy()
end
local u = {}
local v = RaycastParams.new()
v.FilterType = Enum.RaycastFilterType.Exclude
v.IgnoreWater = true
v.RespectCanCollide = true
v.FilterDescendantsInstances = u
local function w()
	v.FilterDescendantsInstances = u
end
local function x(y)
	if y.Name == "HumanoidRootPart" and y:IsA("BasePart") then
		local z = y.Parent
		if z and z:IsA("Model") and not table.find(u, z) then
			table.insert(u, z)
			w()
		end
	end
end
local function z(aa)
	if aa.Name == "HumanoidRootPart" and aa:IsA("BasePart") then
		local ab = aa.Parent
		if ab then
			local ac = table.find(u, ab)
			if ac then
				table.remove(u, ac)
				w()
			end
		end
	end
end
for ad, ae in g(r:GetChildren()) do
	for af, ag in g(ae:GetDescendants()) do
		x(ag)
	end
end
r.DescendantAdded:Connect(x)
r.DescendantRemoving:Connect(z)
local function ah()
	local ai = c:FindFirstChild("Network")
	local aj = ai and ai:FindFirstChild("ServerStatsItem")
	local ak = aj and aj:FindFirstChild("Data Ping")
	if not ak then
		return 0
	end
	local al = ak:GetValue()
	return typeof(al) == "number" and al or 0
end
local function am(an, ao)
	for ap, aq in g(an:GetChildren()) do
		if aq:IsA("BodyVelocity") and aq ~= ao then
			aq:Destroy()
		end
	end
end
local function ar(as, at, au, av, aw)
	local ax = ah() / 1000
	local ay = i(at - ax, 0, math.huge)
	m(ay, function()
		if not as.Parent then
			return
		end
		am(as)
		local az = as.CFrame.LookVector
		local ba = Instance.new("BodyVelocity")
		ba.Name = aw
		ba.MaxForce = Vector3.new(math.huge, 0, math.huge)
		ba.P = math.huge
		ba.Velocity = az * au
		ba.Parent = as
		local bb
		bb = b.Heartbeat:Connect(function()
			if not as.Parent or not ba.Parent then
				bb:Disconnect()
				return
			end
			am(as, ba)
			ba.Velocity = az * au
		end)
		m(av, function()
			if bb then
				bb:Disconnect()
			end
			if ba.Parent then
				ba:Destroy()
			end
		end)
	end)
end
local function bc(bd)
	local be = bd:WaitForChild("HumanoidRootPart")
	local bf = bd:WaitForChild("Humanoid")
	local bg = bf:WaitForChild("Animator")
	local bh = false
	bg.AnimationPlayed:Connect(function(bi)
		local bj = bi.Animation
		if not bj or bh then
			return
		end
		local bk = bj.AnimationId
		if bk ~= n and bk ~= o then
			return
		end
		bh = true
		am(be)
		local bl = (bk == n)
		if bl then
			ar(be, 0.65, 25 / 0.4, 0.4, "DashVelocity")
		else
			ar(be, 0.8, 5 / 0.2, 0.2, "BiteVelocity")
		end
		m(bl and 0.65 or 0.8, function()
			bh = false
		end)
	end)
end
if f.Character then
	bc(f.Character)
end
f.CharacterAdded:Connect(bc)
local function bm(bn)
	local bo = q.e[bn]
	if bo and bo.f then
		bo.f:Disconnect()
	end
	q.e[bn] = nil
end
local function bp(bq, br)
	if bq == f or not br or not br.Parent then
		return
	end
	local bs = br.Parent.Name
	if bs ~= "Survivor" and bs ~= "Killer" then
		return
	end
	local bt = br:WaitForChild("HumanoidRootPart", 5)
	local bu = br:WaitForChild("Humanoid", 5)
	if not bt or not bu then
		return
	end
	bm(bq)
	local bv = bu:WaitForChild("Animator", 5)
	local bw
	if bv then
		bw = bv.AnimationPlayed:Connect(function(bx)
			l(function()
				b.Heartbeat:Wait()
				if bx and bx.IsPlaying then
					local by = bx.Length
					local bz = bx.TimePosition + 0.04
					bx.TimePosition = (by > 0) and i(bz, 0, by) or bz
				end
			end)
		end)
	end
	q.e[bq] = {
		a = br,
		b = bt,
		c = bu,
		d = br:FindFirstChild("Replicator_Good"),
		e = p.d,
		f = bw
	}
end
local function ca(cb)
	if cb == f then
		return
	end
	if cb.Character then
		l(bp, cb, cb.Character)
	end
	cb.CharacterAdded:Connect(function(cc)
		l(bp, cb, cc)
	end)
	cb.CharacterRemoving:Connect(function()
		bm(cb)
	end)
end
for cd, ce in g(a:GetPlayers()) do
	ca(ce)
end
a.PlayerAdded:Connect(ca)
a.PlayerRemoving:Connect(bm)
l(function()
	while k(p.c) do
		local cf = ah()
		if cf > 0 then
			q.c[q.d] = cf
			q.d = q.d % p.b + 1
			local cg = 0
			for ch = 1, p.b do
				cg += q.c[ch]
			end
			local ci = cg / p.b
			q.a = ci
			local cj = f.Character
			local ck = cj and cj.Parent and cj.Parent.Name == "Killer"
			local cl = getgenv().AccurateHurtbox and (ck and 800 or 700) or (ck and 900 or 800)
			q.b = i(ci / cl, 0.04, 0.15)
		end
	end
end)
local cm = Vector3.new(0, - 8, 0)
local cn = Vector3.new(0, 0.5, 0)
b.PreSimulation:Connect(function(co)
	local cp = co * 60
	local cq = f.Character
	if cq and cq.Parent then
		local cr = cq:FindFirstChild("HumanoidRootPart")
		local cs = cq:FindFirstChildOfClass("Humanoid")
		if cr and cs and cr:IsDescendantOf(r) then
			local ct = cr.CFrame
			local cu = ct.Position
			local cv = cr.AssemblyLinearVelocity * q.b
			if cv.Magnitude > p.a then
				cv = cv.Unit * p.a
			end
			local cw = cu + cv
			local cx = cw - cu
			if cx.Magnitude > 0.01 then
				local cy = e:Raycast(cu, cx, v)
				if cy then
					cw = cy.Position + (cy.Normal * 0.2)
				end
			end
			local cz = e:Raycast(cw + cn, cm, v)
			if cz then
				local da = cz.Position.Y + cs.HipHeight + (cr.Size.Y * 0.5)
				if cw.Y < da then
					cw = Vector3.new(cw.X, da, cw.Z)
				end
			end
			local db = ct - cu + cw
			local dc = j((cq:GetAttribute("Stamina") or 0) * 10) / 10
			s:FireServer(db, dc, e:GetServerTimeNow())
		end
	end
	for dd, de in h(q.e) do
		local df = de.a
		local dg = de.b
		local dh = de.d
		if not dd.Parent or not df or not df.Parent or not dg or not dg.Parent then
			bm(dd)
			continue
		end
		if not dh or not dh.Parent then
			dh = df:FindFirstChild("Replicator_Good")
			if not dh then
				continue
			end
			de.d = dh
		end
		local di = (dg.Position - dh.Position).Magnitude
		local dj = i(di / 8.35, p.d, p.e)
		local dk = de.e
		if dj > dk then
			dk = i(dk + p.f, dk, dj)
		else
			dk = i(dk - p.g, dj, dk)
		end
		dk = i(dk, p.d, p.e)
		de.e = dk
		dg.CFrame = dg.CFrame:Lerp(dh.CFrame, 1 - (1 - dk) ^ cp)
	end
end)

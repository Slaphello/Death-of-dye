local a = game:GetService("Players")
local b = game:GetService("ReplicatedStorage")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer
local f = b:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local g = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local h = e:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if h then
	h:Destroy()
end
local i = 0
task.spawn(function()
	local j = {}
	while task.wait(5) do
		local k, l = pcall(function()
			return d.Network.ServerStatsItem["Data Ping"]:GetValue()
		end)
		if k and typeof(l) == "number" then
			table.insert(j, l / 1100)
			if # j > 20 then
				table.remove(j, 1)
			end
			local m = 0
			for n, n in ipairs(j) do
				m += n
			end
			if # j > 0 then
				i = math.clamp(m / # j, 0.02, 0.1)
			end
		end
	end
end)
local function o(p)
	local q = p:FindFirstChild("PosValue")
	if not q then
		return nil
	end
	local r = q.Value
	if typeof(r) == "CFrame" then
		return r
	end
	if typeof(r) == "Instance" then
		if r:IsA("BasePart") then
			return r.CFrame
		end
		if r:IsA("Model") then
			return r:GetPivot()
		end
	end
	return nil
end
local function s()
	for t, t in ipairs(g:GetChildren()) do
		for u, u in ipairs(t:GetChildren()) do
			if u:IsA("Model") and not a:GetPlayerFromCharacter(u) then
				local v = o(u)
				if v then
					u:PivotTo(v)
				end
			end
		end
	end
end
c.RenderStepped:Connect(function(w)
	local x = e.Character
	if not x or not x:IsDescendantOf(g) then
		return
	end
	local y = x:FindFirstChild("HumanoidRootPart")
	if y then
		local z = y.CFrame
		local A = x:GetAttribute("Ragdolled")
		local B = x:GetAttribute("Shiftlock") == false
		local C = x:GetAttribute("UsingAbility") == true
		local D = Vector3.zero
		if not A then
			local E = y.AssemblyLinearVelocity.Magnitude
			if B and C and E > 0.1 then
				D = z.LookVector * E * i
			else
				D = y.AssemblyLinearVelocity * i
			end
		end
		if D.Magnitude > 40 then
			D = D.Unit * 40
		end
		f:FireServer(z + D, x:GetAttribute("Stamina") or 0, workspace:GetServerTimeNow())
		s()
	end
	local F = 1 - ((1 - 0.21) ^ (w * 60))
	for G, G in ipairs(a:GetPlayers()) do
		if G ~= e then
			local H = G.Character
			if H and not H:FindFirstChild("PosValue") then
				local I = H:FindFirstChild("HumanoidRootPart")
				if I then
					local J = H:FindFirstChild("Replicator_Good")
					local K = (J and J:IsA("BasePart")) and J.CFrame or I.CFrame
					I.CFrame = I.CFrame:Lerp(K, F)
				end
			end
		end
	end
end)

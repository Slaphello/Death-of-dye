local a = game:GetService("Players")
local b = game:GetService("ReplicatedStorage")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer;
local b = b:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local f = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local g = e:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if g then
	g:Destroy()
end;
local g = 0;
task.spawn(function()
	local a = {}
	while task.wait(5) do
		local b = d:FindFirstChild("Network") and d.Network:FindFirstChild("ServerStatsItem") and d.Network.ServerStatsItem:FindFirstChild("Data Ping")
		if b and typeof(b:GetValue()) == "number" then
			table.insert(a, b:GetValue() / 1265)
			if # a > 20 then
				table.remove(a, 1)
			end;
			local b = 0;
			for a, a in ipairs(a) do
				b += a
			end;
			if # a > 0 then
				g = math.clamp(b / # a, 0.02, 0.12)
			end
		end
	end
end)
local function d(a)
	local a = a:FindFirstChild("PosValue")
	if not a or not a.Value then
		return nil
	end;
	local a = a.Value;
	if typeof(a) == "CFrame" then
		return a
	elseif typeof(a) == "Instance" and a:IsDescendantOf(workspace) then
		if a:IsA("BasePart") then
			return a.CFrame
		end;
		if a:IsA("Model") then
			return a:GetPivot()
		end
	end;
	return nil
end;
c.RenderStepped:Connect(function(c)
	local h = e.Character;
	if not h or not h:IsDescendantOf(f) then
		return
	end;
	local i = h:FindFirstChild("HumanoidRootPart")
	if i then
		local c = Vector3.zero;
		if not h:GetAttribute("Ragdolled") then
			if h:GetAttribute("Shiftlock") == false and h:GetAttribute("UsingAbility") == true and i.AssemblyLinearVelocity.Magnitude > 0.1 then
				c = i.CFrame.LookVector * i.AssemblyLinearVelocity.Magnitude * g
			else
				c = i.AssemblyLinearVelocity * g
			end
		end;
		if c.Magnitude > 40 then
			c = c.Unit * 40
		end;
		b:FireServer(i.CFrame + c, h:GetAttribute("Stamina") or 0, workspace:GetServerTimeNow() - g)
		for b, b in ipairs(f:GetChildren()) do
			for b, b in ipairs(b:GetChildren()) do
				if b:IsA("Model") and not a:GetPlayerFromCharacter(b) then
					local a = d(b)
					if a then
						b:PivotTo(a)
					end
				end
			end
		end
	end;
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= e and a.Character and not a.Character:FindFirstChild("PosValue") then
			local b = a.Character:FindFirstChild("HumanoidRootPart")
			if b then
				local a = a.Character:FindFirstChild("Replicator_Good")
				local a = (a and a:IsA("BasePart")) and a.CFrame or b.CFrame;
				if a then
					local d = - (math.cos(math.pi * math.clamp((b.Position - a.Position).Magnitude / 9, 0.01, 1)) - 1) / 2;
					local c = math.clamp((1 - ((0.68 + g) ^ (c * 60))) * d, 0.18, 0.8)
					b.CFrame = b.CFrame:Lerp(a, c)
				end
			end
		end
	end
end)

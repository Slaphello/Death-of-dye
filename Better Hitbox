local a = game:GetService("Players")
local b = game:GetService("ReplicatedStorage")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer;
local b = b:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local f = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local g = e:WaitForChild("PlayerScripts")
local g = g:FindFirstChild("ClientReplicator")
if g then
	g:Destroy()
end;
local g = 0;
task.spawn(function()
	local a = {}
	while task.wait(5) do
		table.insert(a, d.Network.ServerStatsItem["Data Ping"]:GetValue() / 1250)
		if # a > 20 then
			table.remove(a, 1)
		end;
		local b = 0;
		for a, a in ipairs(a) do
			b = b + a
		end;
		g = math.clamp(b / # a, 0.04, 0.2)
	end
end)
local d = 0;
c.RenderStepped:Connect(function(c)
	d = d + c;
	local c = e.Character;
	if not c or not c:IsDescendantOf(f) then
		return
	end;
	if d >= 0.0175 then
		d = 0;
		local d = c:FindFirstChild("HumanoidRootPart")
		if d then
			local e = c:FindFirstChild("PosValue")
			if e and e.Value then
				d:PivotTo(e.Value.CFrame)
			end;
			local e = d.CFrame;
			local h = c:GetAttribute("Ragdolled")
			local i = c:GetAttribute("Shiftlock") == false;
			local j = c:GetAttribute("UsingAbility") == true;
			local k = Vector3.zero;
			if not h then
				local a = d.AssemblyLinearVelocity.Magnitude;
				if i and j and a > 0.1 then
					k = e.LookVector * a * g
				else
					k = d.AssemblyLinearVelocity * g
				end
			end;
			if k.Magnitude > 40 then
				k = k.Unit * 40
			end;
			b:FireServer(e + k, c:GetAttribute("Stamina") or 0, workspace:GetServerTimeNow())
			for b, b in ipairs(f:GetChildren()) do
				for b, b in ipairs(b:GetChildren()) do
					if not a:GetPlayerFromCharacter(b) then
						local a = b:FindFirstChild("PosValue")
						if a and b.PrimaryPart then
							b.PrimaryPart:PivotTo(a.Value.CFrame)
						end
					end
				end
			end
		end
	end;
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= e then
			local a = a.Character;
			if a and not a:FindFirstChild("PosValue") then
				local b = a:FindFirstChild("HumanoidRootPart")
				if b then
					local a = a:FindFirstChild("Replicator_Good")
					b.CFrame = b.CFrame:Lerp(a and a.CFrame or b.CFrame, 0.16)
				end
			end
		end
	end
end)

local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer;
local g = ipairs;
local h = pairs;
local i = math.clamp;
local j = math.min;
local k = math.max;
local l = math.floor;
local m = task.wait;
local n = task.spawn;
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end;
local o = {
	M_VLO = 99,
	AvgPing = 60,
	PUI = 2,
	NnL = 0.14,
	MxL = 0.24,
	LPS = 0.001,
	LPF = 0.0002
}
local p = {
	PTime = 0.035,
	PH = table.create(o.AvgPing, 0),
	PIdex = 1,
	TdP = {}
}
local q = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local d = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local r = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if r then
	r:Destroy()
end;
n(function()
	local a;
	while m(o.PUI) do
		if not a then
			local b = c:FindFirstChild("Network")
			local b = b and b:FindFirstChild("ServerStatsItem")
			a = b and b:FindFirstChild("Data Ping")
		end;
		if not a then
			continue
		end;
		local a = a:GetValue()
		if typeof(a) ~= "number" then
			continue
		end;
		p.PH[p.PIdex] = a;
		p.PIdex = p.PIdex % o.AvgPing + 1;
		local a = 0;
		for b = 1, o.AvgPing do
			a += p.PH[b]
		end;
		local a = a / o.AvgPing;
		local b = getgenv().AccurateHurtbox == true and 750 or 1000;
		p.PTime = i(a / b, 0.035, 0.145)
	end
end)
local function c(a, b)
	if a == f or not b or not b.Parent then
		return
	end;
	local c = b.Parent.Name;
	if c ~= "Survivor" and c ~= "Killer" then
		return
	end;
	local c = b:WaitForChild("HumanoidRootPart", 5)
	if not c then
		return
	end;
	p.TdP[a] = {
		Character = b,
		HRP = c,
		Repli = b:FindFirstChild("Replicator_Good"),
		Lerp = o.NnL
	}
end;
local function m(a)
	if a == f then
		return
	end;
	if a.Character then
		n(c, a, a.Character)
	end;
	a.CharacterAdded:Connect(function(b)
		n(c, a, b)
	end)
	a.CharacterRemoving:Connect(function()
		p.TdP[a] = nil
	end)
end;
for a, a in g(a:GetPlayers()) do
	m(a)
end;
a.PlayerAdded:Connect(m)
a.PlayerRemoving:Connect(function(a)
	p.TdP[a] = nil
end)
local a = RaycastParams.new()
a.FilterType = Enum.RaycastFilterType.Exclude;
a.IgnoreWater = true;
a.RespectCanCollide = true;
b.RenderStepped:Connect(function(b)
	local b = b * 60;
	local c = f.Character;
	if c and c.Parent then
		local b = c:FindFirstChild("HumanoidRootPart")
		local f = c:FindFirstChildOfClass("Humanoid")
		if b and f and b:IsDescendantOf(q) then
			local g = b.CFrame;
			local h = g.Position;
			local i = b.AssemblyLinearVelocity * p.PTime;
			if i.Magnitude > o.M_VLO then
				i = i.Unit * o.M_VLO
			end;
			local i = h + i;
			a.FilterDescendantsInstances = {
				c
			}
			local b = f.HipHeight + (b.Size.Y / 2)
			local f = i - h;
			if f.Magnitude > 0.01 then
				local a = e:Raycast(h, f, a)
				if a then
					i = a.Position + (a.Normal * 0.2)
				end
			end;
			local f = Vector3.new(i.X, i.Y + 5, i.Z)
			local j = Vector3.new(0, - 20, 0)
			local a = e:Raycast(f, j, a)
			if a then
				local a = a.Position.Y + b;
				if i.Y < a then
					i = Vector3.new(i.X, a, i.Z)
				end
			end;
			local a = g - h + i;
			local b = c:GetAttribute("Stamina") or 0;
			b = l(b * 10) / 10;
			d:FireServer(a, b, e:GetServerTimeNow())
		end
	end;
	for a, c in h(p.TdP) do
		local d = c.Character;
		local e = c.HRP;
		local f = c.Repli;
		if not a.Parent or not d or not d.Parent or not e or not e.Parent then
			p.TdP[a] = nil;
			continue
		end;
		if not f or not f.Parent then
			f = d:FindFirstChild("Replicator_Good")
			if f then
				c.Repli = f
			else
				continue
			end
		end;
		local a = (e.Position - f.Position).Magnitude;
		local a = i(a / 8.35, o.NnL, o.MxL)
		local d = c.Lerp;
		if a > d then
			d = j(d + o.LPS, a)
		elseif a < d then
			d = k(d - o.LPF, a)
		end;
		d = i(d, o.NnL, o.MxL)
		c.Lerp = d;
		local a = 1 - (1 - d) ^ b;
		e.CFrame = e.CFrame:Lerp(f.CFrame, a)
	end
end)

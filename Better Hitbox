local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer
local g = ipairs
local h = pairs
local i = math.clamp
local j = math.floor
local k = task.wait
local l = task.spawn
local m = task.delay
local n = "rbxassetid://125600990923275"
local o = "rbxassetid://127982599288788"
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end
local p = {
	a = 99,
	b = 10,
	c = 2,
	d = 0.14,
	e = 0.24,
	f = 0.001,
	g = 0.0002
}
local q = {
	a = 40,
	b = 0.035,
	c = table.create(p.b, 0),
	d = 1,
	e = {}
}
local r = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local s = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local t = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if t then
	t:Destroy()
end
local u = {}
local v = RaycastParams.new()
v.FilterType = Enum.RaycastFilterType.Exclude
v.IgnoreWater = true
v.RespectCanCollide = true
v.FilterDescendantsInstances = u
local function w()
	v.FilterDescendantsInstances = u
end
local function x(y)
	if y.Name == "HumanoidRootPart" and y:IsA("BasePart") then
		local z = y.Parent
		if z and z:IsA("Model") and not table.find(u, z) then
			table.insert(u, z)
			w()
		end
	end
end
local function z(aa)
	if aa.Name == "HumanoidRootPart" and aa:IsA("BasePart") then
		local ab = aa.Parent
		if ab then
			local ac = table.find(u, ab)
			if ac then
				table.remove(u, ac)
				w()
			end
		end
	end
end
for ad, ae in g(r:GetChildren()) do
	for af, ag in g(ae:GetDescendants()) do
		x(ag)
	end
end
r.DescendantAdded:Connect(x)
r.DescendantRemoving:Connect(z)
local function ah()
	local ai = c:FindFirstChild("Network")
	local aj = ai and ai:FindFirstChild("ServerStatsItem")
	local ak = aj and aj:FindFirstChild("Data Ping")
	if not ak then
		return 0
	end
	local al = ak:GetValue()
	return typeof(al) == "number" and al or 0
end
local function am(an, ao)
	for ap, aq in g(an:GetChildren()) do
		if aq:IsA("BodyVelocity") and aq ~= ao then
			aq:Destroy()
		end
	end
end
local function ar(as, at, au, av, aw)
	local ax = i(at - (q.a / 1000), 0, math.huge)
	m(ax, function()
		if not as.Parent then
			return
		end
		am(as)
		local ay = Instance.new("BodyVelocity")
		ay.Name = aw
		ay.MaxForce = Vector3.new(math.huge, 0, math.huge)
		ay.P = math.huge
		ay.Velocity = as.CFrame.LookVector * au
		ay.Parent = as
		m(av, function()
			if ay.Parent then
				ay:Destroy()
			end
		end)
	end)
end
local function ax(ay)
	local az = ay:WaitForChild("HumanoidRootPart")
	local ba = ay:WaitForChild("Humanoid")
	local bb = ba:WaitForChild("Animator")
	local bc = false
	bb.AnimationPlayed:Connect(function(bd)
		local be = bd.Animation
		if not be or bc then
			return
		end
		local bf = be.AnimationId
		if bf ~= n and bf ~= o then
			return
		end
		bc = true
		am(az)
		local bg = (bf == n)
		if bg then
			ar(az, 0.65, 62.5, 0.4, "DashVelocity")
		else
			ar(az, 0.8, 25, 0.2, "BiteVelocity")
		end
		m(bg and 0.65 or 0.8, function()
			bc = false
		end)
	end)
end
if f.Character then
	ax(f.Character)
end
f.CharacterAdded:Connect(ax)
local function bh(bi)
	local bj = q.e[bi]
	if bj and bj.g then
		bj.g:Disconnect()
	end
	q.e[bi] = nil
end
local function bk(bl, bm)
	if bl == f or not bm or not bm.Parent then
		return
	end
	local bn = bm.Parent.Name
	if bn ~= "Survivor" and bn ~= "Killer" then
		return
	end
	local bo = bm:WaitForChild("HumanoidRootPart", 5)
	local bp = bm:WaitForChild("Humanoid", 5)
	if not bo or not bp then
		return
	end
	bh(bl)
	local bq = bp:WaitForChild("Animator", 5)
	local br
	if bq then
		br = bq.AnimationPlayed:Connect(function(bs)
			l(function()
				b.Heartbeat:Wait()
				if bs and bs.IsPlaying then
					local bt = bs.Length
					local bu = bs.TimePosition + 0.04
					bs.TimePosition = (bt > 0) and i(bu, 0, bt) or bu
				end
			end)
		end)
	end
	q.e[bl] = {
		a = bm,
		b = bo,
		c = bp,
		d = bm:FindFirstChild("Replicator_Good"),
		e = p.d,
		g = br
	}
end
local function bv(bw)
	if bw == f then
		return
	end
	if bw.Character then
		l(bk, bw, bw.Character)
	end
	bw.CharacterAdded:Connect(function(bx)
		l(bk, bw, bx)
	end)
	bw.CharacterRemoving:Connect(function()
		bh(bw)
	end)
end
for by, bz in g(a:GetPlayers()) do
	bv(bz)
end
a.PlayerAdded:Connect(bv)
a.PlayerRemoving:Connect(bh)
l(function()
	while k(p.c) do
		local ca = ah()
		if ca > 0 then
			q.c[q.d] = ca
			q.d = q.d % p.b + 1
			local cb = 0
			for cc = 1, p.b do
				cb += q.c[cc]
			end
			local cd = cb / p.b
			q.a = cd
			local ce = f.Character
			local cf = ce and ce.Parent and ce.Parent.Name == "Killer"
			local cg = getgenv().AccurateHurtbox and (cf and 835 or 735) or (cf and 935 or 835)
			q.b = i(cd / cg, 0.04, 0.15)
		end
	end
end)
local ch = Vector3.new(0, - 8, 0)
local ci = Vector3.new(0, 0.5, 0)
b.PreSimulation:Connect(function(cj)
	local ck = cj * 60
	local cl = f.Character
	if cl and cl.Parent then
		local cm = cl:FindFirstChild("HumanoidRootPart")
		local cn = cl:FindFirstChildOfClass("Humanoid")
		if cm and cn and cm:IsDescendantOf(r) then
			local co = cm.CFrame
			local cp = co.Position
			local cq = cm.AssemblyLinearVelocity * q.b
			if cq.Magnitude > p.a then
				cq = cq.Unit * p.a
			end
			local cr = cp + cq
			local cs = cr - cp
			if cs.Magnitude > 0.01 then
				local ct = e:Raycast(cp, cs, v)
				if ct then
					cr = ct.Position + (ct.Normal * 0.2)
				end
			end
			local cu = e:Raycast(cr + ci, ch, v)
			if cu then
				local cv = cu.Position.Y + cn.HipHeight + (cm.Size.Y * 0.5)
				if cr.Y < cv then
					cr = Vector3.new(cr.X, cv, cr.Z)
				end
			end
			local cw = co - cp + cr
			local cx = j((cl:GetAttribute("Stamina") or 0) * 10) / 10
			s:FireServer(cw, cx, e:GetServerTimeNow())
		end
	end
	for cy, cz in h(q.e) do
		local da = cz.a
		local db = cz.b
		local dc = cz.d
		if not cy.Parent or not da or not da.Parent or not db or not db.Parent then
			bh(cy)
			continue
		end
		if not dc or not dc.Parent then
			dc = da:FindFirstChild("Replicator_Good")
			if not dc then
				continue
			end
			cz.d = dc
		end
		local dd = (db.Position - dc.Position).Magnitude
		local de = i(dd / 8.35, p.d, p.e)
		local df = cz.e
		if de > df then
			df = i(df + p.f, df, de)
		else
			df = i(df - p.g, de, df)
		end
		df = i(df, p.d, p.e)
		cz.e = df
		db.CFrame = db.CFrame:Lerp(dc.CFrame, 1 - (1 - df) ^ ck)
	end
end)

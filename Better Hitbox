local a = game:GetService("Players")
local b = game:GetService("ReplicatedStorage")
local c = game:GetService("RunService")
local d = game:GetService("Stats")
local e = a.LocalPlayer;
local b = b:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local f = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local g = e:WaitForChild("PlayerScripts")
local g = g:FindFirstChild("ClientReplicator")
if g then
	g:Destroy()
end;
local g = 0.02;
task.spawn(function()
	local a = {}
	while task.wait(5) do
		local b, c = pcall(function()
			return d.Network.ServerStatsItem["Data Ping"]:GetValue()
		end)
		if b and typeof(c) == "number" then
			table.insert(a, c / 1850)
			if # a > 20 then
				table.remove(a, 1)
			end;
			local b = 0;
			for a, a in ipairs(a) do
				b += a
			end;
			if # a > 0 then
				g = math.clamp(b / # a, 0.02, 0.1)
			end
		end
	end
end)
local function d(a)
	local a = a:FindFirstChild("PosValue")
	if not a then
		return nil
	end;
	local a = a.Value;
	if typeof(a) == "CFrame" then
		return a
	end;
	if typeof(a) == "Instance" then
		if a:IsA("BasePart") then
			return a.CFrame
		end;
		if a:IsA("Model") then
			return a:GetPivot()
		end
	end;
	return nil
end;
local function h(a)
	local b = d(a)
	if not b then
		return
	end;
	if a:IsA("Model") then
		a:PivotTo(b)
	end
end;
local function i()
	for b, b in ipairs(f:GetChildren()) do
		for b, b in ipairs(b:GetChildren()) do
			if b:IsA("Model") then
				local a = a:GetPlayerFromCharacter(b)
				if not a then
					local a = d(b)
					if a then
						b:PivotTo(a)
					end
				end
			end
		end
	end
end;
c.RenderStepped:Connect(function(c)
	local d = e.Character;
	if not d or not d:IsDescendantOf(f) then
		return
	end;
	local f = d:FindFirstChild("HumanoidRootPart")
	if f then
		h(d)
		local a = f.CFrame;
		local c = d:GetAttribute("Ragdolled")
		local e = d:GetAttribute("Shiftlock") == false;
		local h = d:GetAttribute("UsingAbility") == true;
		local j = Vector3.zero;
		if not c then
			local b = f.AssemblyLinearVelocity.Magnitude;
			if e and h and b > 0.1 then
				j = a.LookVector * b * g
			else
				j = f.AssemblyLinearVelocity * g
			end
		end;
		if j.Magnitude > 40 then
			j = j.Unit * 40
		end;
		b:FireServer(a + j, d:GetAttribute("Stamina") or 0, workspace:GetServerTimeNow())
		i()
	end;
	local b = 1 - ((1 - 0.148) ^ (c * 60))
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= e then
			local a = a.Character;
			if a and not a:FindFirstChild("PosValue") then
				local c = a:FindFirstChild("HumanoidRootPart")
				if c then
					local a = a:FindFirstChild("Replicator_Good")
					local a = a and a:IsA("BasePart") and a.CFrame or c.CFrame;
					c.CFrame = c.CFrame:Lerp(a, b)
				end
			end
		end
	end
end)

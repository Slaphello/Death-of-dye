local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Stats")
local d = game:GetService("ReplicatedStorage")
local e = game:GetService("Workspace")
local f = a.LocalPlayer
local g = ipairs
local h = pairs
local i = math.clamp
local j = math.min
local k = math.max
local l = math.floor
local m = task.wait
local n = task.spawn
if getgenv().AccurateHurtbox == nil then
	getgenv().AccurateHurtbox = true
end
local o = {
	p = 99,
	q = 10,
	r = 2,
	s = 0.14,
	t = 0.2,
	u = 0.001,
	v = 0.00025
}
local w = {
	x = 50,
	y = 0.035,
	z = table.create(o.q, 0),
	aa = 1,
	ab = {}
}
local ac = Vector3.new(0, - 8, 0)
local ad = Vector3.new(0, 0.5, 0)
local ae = e:WaitForChild("GameAssets"):WaitForChild("Teams")
local af = d:WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local ag = f:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if ag then
	ag:Destroy()
end
local ah = {}
local ai = RaycastParams.new()
ai.FilterType = Enum.RaycastFilterType.Exclude
ai.IgnoreWater = true
ai.RespectCanCollide = true
local function aj()
	ai.FilterDescendantsInstances = ah
end
local function ak(al)
	if al.Name == "HumanoidRootPart" and al:IsA("BasePart") then
		local am = al.Parent
		if am and am:IsA("Model") and not table.find(ah, am) then
			table.insert(ah, am)
			aj()
		end
	end
end
local function an(al)
	if al.Name == "HumanoidRootPart" and al:IsA("BasePart") then
		local am = al.Parent
		if am then
			local ao = table.find(ah, am)
			if ao then
				table.remove(ah, ao)
				aj()
			end
		end
	end
end
for ap, aq in g(ae:GetChildren()) do
	for ap, ar in g(aq:GetDescendants()) do
		ak(ar)
	end
end
ae.DescendantAdded:Connect(ak)
ae.DescendantRemoving:Connect(an)
local function as(at)
	w.ab[at] = nil
end
local function ax(at, ay)
	if at == f or not ay or not ay.Parent then
		return
	end
	local az = ay.Parent.Name
	if az ~= "Survivor" and az ~= "Killer" then
		return
	end
	local ba = ay:WaitForChild("HumanoidRootPart", 5)
	local bb = ay:WaitForChild("Humanoid", 5)
	if not ba or not bb then
		return
	end
	as(at)
	local bc = {
		Character = ay,
		HRP = ba,
		Humanoid = bb,
		Repli = ay:FindFirstChild("Replicator_Good"),
		Lerp = o.s
	}
	w.ab[at] = bc
end
local function bd(at)
	if at == f then
		return
	end
	if at.Character then
		n(ax, at, at.Character)
	end
	at.CharacterAdded:Connect(function(be)
		n(ax, at, be)
	end)
	at.CharacterRemoving:Connect(function()
		as(at)
	end)
end
for ap, bf in g(a:GetPlayers()) do
	bd(bf)
end
a.PlayerAdded:Connect(bd)
a.PlayerRemoving:Connect(as)
n(function()
	local bg = nil
	while m(o.r) do
		if not bg then
			local bh = c:FindFirstChild("Network")
			local bi = bh and bh:FindFirstChild("ServerStatsItem")
			bg = bi and bi:FindFirstChild("Data Ping")
		end
		if bg then
			local bj = bg:GetValue()
			if typeof(bj) == "number" then
				w.z[w.aa] = bj
				w.aa = w.aa % o.q + 1
				local bk = 0
				for bl = 1, o.q do
					bk += w.z[bl]
				end
				local bm = bk / o.q
				w.x = bm
				local bn = f.Character
				local bo = (bn and bn.Parent and bn.Parent.Name == "Killer")
				local bp = getgenv().AccurateHurtbox and (bo and 800 or 700) or (bo and 900 or 800)
				w.y = i(bm / bp, 0.04, 0.15)
			end
		end
	end
end)
b.PreSimulation:Connect(function(bq)
	local br = bq * 60
	local bs = f.Character
	if bs and bs.Parent then
		local bt = bs:FindFirstChild("HumanoidRootPart")
		local bu = bs:FindFirstChildOfClass("Humanoid")
		if bt and bu and bt:IsDescendantOf(ae) then
			local bv = bt.CFrame
			local bw = bv.Position
			local bx = bt.AssemblyLinearVelocity * w.y
			if bx.Magnitude > o.p then
				bx = bx.Unit * o.p
			end
			local by = bw + bx
			local bz = by - bw
			if bz.Magnitude > 0.01 then
				local ca = e:Raycast(bw, bz, ai)
				if ca then
					by = ca.Position + (ca.Normal * 0.2)
				end
			end
			local cb = e:Raycast(by + ad, ac, ai)
			if cb then
				local cc = cb.Position.Y + bu.HipHeight + (bt.Size.Y * 0.5)
				if by.Y < cc then
					by = Vector3.new(by.X, cc, by.Z)
				end
			end
			local cd = bv - bw + by
			local ce = l((bs:GetAttribute("Stamina") or 0) * 10) / 10
			af:FireServer(cd, ce, e:GetServerTimeNow())
		end
	end
	for cf, cg in h(w.ab) do
		local ch = cg.Character
		local ci = cg.HRP
		local cj = cg.Repli
		if not cf.Parent or not ch or not ch.Parent or not ci or not ci.Parent then
			as(cf)
			continue
		end
		if not cj or not cj.Parent then
			cj = ch:FindFirstChild("Replicator_Good")
			if cj then
				cg.Repli = cj
			else
				continue
			end
		end
		local ck = (ci.Position - cj.Position).Magnitude
		local cl = i(ck / 8.35, o.s, o.t)
		local cm = cg.Lerp
		if cl > cm then
			cm = j(cm + o.u, cl)
		elseif cl < cm then
			cm = k(cm - o.v, cl)
		end
		cm = i(cm, o.s, o.t)
		cg.Lerp = cm
		local cn = 1 - (1 - cm) ^ br
		ci.CFrame = ci.CFrame:Lerp(cj.CFrame, cn)
	end
end)

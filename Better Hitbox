local a = game:GetService("Players")
local b = a.LocalPlayer;
local c = workspace:WaitForChild("GameAssets"):WaitForChild("Teams")
local d = game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("RemoteEvents"):WaitForChild("CustomReplication"):WaitForChild("UpdateServerPos")
local e = b:WaitForChild("PlayerScripts"):FindFirstChild("ClientReplicator")
if e then
	e:Destroy()
end;
local e, f = 0, {}
task.spawn(function()
	local a = game:GetService("Stats"):FindFirstChild("Network")
	while task.wait(5) do
		local a = a and a:FindFirstChild("ServerStatsItem") and a.ServerStatsItem:FindFirstChild("Data Ping")
		if a and typeof(a:GetValue()) == "number" then
			table.insert(f, a:GetValue() / 1265)
			if # f > 20 then
				table.remove(f, 1)
			end;
			local a = 0;
			for b, b in ipairs(f) do
				a += b
			end;
			if # f > 0 then
				e = math.clamp(a / # f, 0.02, 0.12)
			end
		end
	end
end)
game:GetService("RunService").RenderStepped:Connect(function()
	local f = b.Character;
	if not f or not f:IsDescendantOf(c) then
		return
	end;
	local g = f:FindFirstChild("HumanoidRootPart")
	if g then
		local b = Vector3.zero;
		if not f:GetAttribute("Ragdolled") then
			if f:GetAttribute("Shiftlock") == false and f:GetAttribute("UsingAbility") == true and g.AssemblyLinearVelocity.Magnitude > 0.1 then
				b = g.CFrame.LookVector * g.AssemblyLinearVelocity.Magnitude * e
			else
				b = g.AssemblyLinearVelocity * e
			end
		end;
		if b.Magnitude > 40 then
			b = b.Unit * 40
		end;
		d:FireServer(g.CFrame + b, f:GetAttribute("Stamina") or 0, workspace:GetServerTimeNow() - e)
		for b, b in ipairs(c:GetChildren()) do
			for b, b in ipairs(b:GetChildren()) do
				if b:IsA("Model") and not a:GetPlayerFromCharacter(b) then
					local a = b:FindFirstChild("PosValue")
					if a and a.Value then
						local a = a.Value;
						if typeof(a) == "CFrame" then
							b:PivotTo(a)
						elseif typeof(a) == "Instance" and a:IsDescendantOf(workspace) then
							if a:IsA("BasePart") then
								b:PivotTo(a.CFrame)
							elseif a:IsA("Model") then
								b:PivotTo(a:GetPivot())
							end
						end
					end
				end
			end
		end
	end;
	for a, a in ipairs(a:GetPlayers()) do
		if a ~= b and a.Character and not a.Character:FindFirstChild("PosValue") then
			local b = a.Character:FindFirstChild("HumanoidRootPart")
			if b then
				local a = a.Character:FindFirstChild("Replicator_Good")
				local a = (a and a:IsA("BasePart")) and a.CFrame or b.CFrame;
				if a then
					local c = (b.Position - a.Position).Magnitude;
					b.CFrame = b.CFrame:Lerp(a, math.clamp(c / 3.1, 0.05, 0.25))
				end
			end
		end
	end
end)

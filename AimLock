local a = loadstring(game:HttpGet("https://raw.githubusercontent.com/Slaphello/Death-of-dye/refs/heads/main/P%20Module"))()
local b = game:GetService("Players")
local c = game:GetService("UserInputService")
local d = game:GetService("CoreGui")
local b = b.LocalPlayer;
local e = getgenv().Aimlock_Key_Toggle or Enum.KeyCode.R;
local f = getgenv().Aimlock_Key_ToggleMulti or Enum.KeyCode.T;
local g = {
	RN = Color3.fromRGB(140, 35, 35),
	RB = Color3.fromRGB(255, 50, 50),
	YN = Color3.fromRGB(160, 135, 20),
	YB = Color3.fromRGB(255, 235, 59),
	BN = Color3.fromRGB(30, 70, 140),
	BB = Color3.fromRGB(0, 180, 255),
	GN = Color3.fromRGB(35, 120, 35),
	GB = Color3.fromRGB(50, 220, 50)
}
local function h(a, b)
	local d = false;
	local e;
	local f;
	local g;
	a.InputBegan:Connect(function(c)
		if b and b() then
			return
		end;
		if c.UserInputType == Enum.UserInputType.MouseButton1 or c.UserInputType == Enum.UserInputType.Touch then
			d = true;
			e = c.Position;
			f = a.Position;
			c.Changed:Connect(function()
				if c.UserInputState == Enum.UserInputState.End then
					d = false
				end
			end)
		end
	end)
	a.InputChanged:Connect(function(a)
		if a.UserInputType == Enum.UserInputType.MouseMovement or a.UserInputType == Enum.UserInputType.Touch then
			g = a
		end
	end)
	c.InputChanged:Connect(function(c)
		if b and b() then
			return
		end;
		if c == g and d then
			local b = c.Position - e;
			a.Position = UDim2.new(f.X.Scale, f.X.Offset + b.X, f.Y.Scale, f.Y.Offset + b.Y)
		end
	end)
end;
local function i(a, c)
	local b = b.Character;
	local b = b and b:FindFirstChildOfClass("Humanoid")
	if b then
		b.AutoRotate = not (c and a == "HRP")
	end
end;
local function j()
	local j = d:FindFirstChild("AimLockUI")
	if j then
		j:Destroy()
	end;
	local j = false;
	local k = Instance.new("ScreenGui")
	k.Name = "AimLockUI";
	k.ResetOnSpawn = false;
	pcall(function()
		k.Parent = d
	end)
	if not k.Parent then
		k.Parent = b:WaitForChild("PlayerGui")
	end;
	local function b(a, b, c, d)
		local e = Instance.new("TextButton")
		e.Name = a;
		e.Size = UDim2.new(0, 80, 0, 35)
		e.Position = d;
		e.BackgroundColor3 = c;
		e.BackgroundTransparency = 0.4;
		e.Text = b;
		e.TextColor3 = Color3.new(1, 1, 1)
		e.Font = Enum.Font.GothamBold;
		e.TextSize = 12;
		e.Parent = k;
		local a = Instance.new("UICorner")
		a.CornerRadius = UDim.new(0, 6)
		a.Parent = e;
		local a = Instance.new("UIStroke")
		a.Color = Color3.new(0, 0, 0)
		a.Thickness = 1;
		a.Transparency = 0.6;
		a.Parent = e;
		h(e, function()
			return j
		end)
		return e
	end;
	local d = b("BtnToggle", "AIM: OFF", g.RN, UDim2.new(0.5, - 175, 0, 20))
	local h = b("BtnMode", "MODE: CAM", g.YN, UDim2.new(0.5, - 85, 0, 20))
	local k = b("BtnMulti", "MULTI: OFF", g.BN, UDim2.new(0.5, 5, 0, 20))
	local b = b("BtnLockUI", "UI: UNLOCKED", g.GN, UDim2.new(0.5, 95, 0, 20))
	local l = "CAM";
	local m = false;
	local function n(a)
		m = a;
		d.Text = a and "AIM: ON" or "AIM: OFF";
		d.BackgroundColor3 = a and g.RB or g.RN
	end;
	local function o(a)
		l = a;
		h.Text = "MODE: " .. a;
		h.BackgroundColor3 = (a == "HRP") and g.YB or g.YN
	end;
	local function p(a)
		k.Text = a and "MULTI: ON" or "MULTI: OFF";
		k.BackgroundColor3 = a and g.BB or g.BN
	end;
	d.MouseButton1Click:Connect(function()
		local a = a:TA()
		n(a)
		i(l, a)
	end)
	h.MouseButton1Click:Connect(function()
		local a = a:TMode()
		o(a)
		i(a, m)
	end)
	k.MouseButton1Click:Connect(function()
		local a = a:TM()
		p(a)
	end)
	b.MouseButton1Click:Connect(function()
		j = not j;
		b.Text = j and "UI: LOCKED" or "UI: UNLOCKED";
		b.BackgroundColor3 = j and g.GN or g.GB
	end)
	c.InputBegan:Connect(function(b, c)
		if c then
			return
		end;
		if b.KeyCode == e then
			local a = a:TA()
			n(a)
			i(l, a)
		elseif b.KeyCode == f then
			local a = a:TM()
			p(a)
		end
	end)
	n(false)
	o("CAM")
	p(false)
	i("CAM", false)
end;
j()

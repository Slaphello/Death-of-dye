local a = loadstring(game:HttpGet("https://raw.githubusercontent.com/Slaphello/Death-of-dye/refs/heads/main/P%20Module"))()
local b = getgenv().Aimlock_Key_Toggle or Enum.KeyCode.R;
local c = getgenv().Aimlock_Key_ToggleMulti or Enum.KeyCode.T;
local d = game:GetService("Players")
local e = game:GetService("UserInputService")
local f = game:GetService("CoreGui")
local d = d.LocalPlayer;
local g = {
	RN = Color3.fromRGB(140, 35, 35),
	RB = Color3.fromRGB(255, 50, 50),
	YN = Color3.fromRGB(160, 135, 20),
	YB = Color3.fromRGB(255, 235, 59),
	BN = Color3.fromRGB(30, 70, 140),
	BB = Color3.fromRGB(0, 180, 255),
	GN = Color3.fromRGB(35, 120, 35),
	GB = Color3.fromRGB(50, 220, 50)
}
local function h(a, b)
	local c, d, f, g;
	a.InputBegan:Connect(function(d)
		if b and b() then
			return
		end;
		if d.UserInputType == Enum.UserInputType.MouseButton1 or d.UserInputType == Enum.UserInputType.Touch then
			c = true;
			f = d.Position;
			g = a.Position;
			d.Changed:Connect(function()
				if d.UserInputState == Enum.UserInputState.End then
					c = false
				end
			end)
		end
	end)
	a.InputChanged:Connect(function(a)
		if a.UserInputType == Enum.UserInputType.MouseMovement or a.UserInputType == Enum.UserInputType.Touch then
			d = a
		end
	end)
	e.InputChanged:Connect(function(e)
		if b and b() then
			return
		end;
		if e == d and c then
			local b = e.Position - f;
			a.Position = UDim2.new(g.X.Scale, g.X.Offset + b.X, g.Y.Scale, g.Y.Offset + b.Y)
		end
	end)
end;
local function i(a)
	local b = d.Character;
	local b = b and b:FindFirstChildOfClass("Humanoid")
	if b then
		b.AutoRotate = (a ~= "HRP")
	end
end;
local function j()
	if f:FindFirstChild("AimLockUI") then
		f.AimLockUI:Destroy()
	end;
	local j = false;
	local k = Instance.new("ScreenGui")
	k.Name = "AimLockUI";
	k.ResetOnSpawn = false;
	pcall(function()
		k.Parent = f
	end)
	if not k.Parent then
		k.Parent = d:WaitForChild("PlayerGui")
	end;
	local function d(a, b, c, d)
		local e = Instance.new("TextButton")
		e.Name = a;
		e.Size = UDim2.new(0, 80, 0, 35)
		e.Position = d;
		e.BackgroundColor3 = c;
		e.BackgroundTransparency = 0.4;
		e.Text = b;
		e.TextColor3 = Color3.new(1, 1, 1)
		e.Font = Enum.Font.GothamBold;
		e.TextSize = 12;
		e.Parent = k;
		Instance.new("UICorner", e).CornerRadius = UDim.new(0, 6)
		local a = Instance.new("UIStroke")
		a.Color = Color3.fromRGB(0, 0, 0)
		a.Thickness = 1;
		a.Transparency = 0.6;
		a.Parent = e;
		h(e, function()
			return j
		end)
		return e
	end;
	local f = d("BtnToggle", "AIM: OFF", g.RN, UDim2.new(0.5, - 175, 0, 20))
	local h = d("BtnMode", "MODE: CAM", g.YN, UDim2.new(0.5, - 85, 0, 20))
	local k = d("BtnMulti", "MULTI: OFF", g.BN, UDim2.new(0.5, 5, 0, 20))
	local d = d("BtnLockUI", "UI: UNLOCKED", g.GN, UDim2.new(0.5, 95, 0, 20))
	local function l(a)
		f.Text = a and "AIM: ON" or "AIM: OFF";
		f.BackgroundColor3 = a and g.RB or g.RN
	end;
	local function m(a)
		h.Text = "MODE: " .. a;
		if a == "HRP" then
			h.BackgroundColor3 = g.YB
		else
			h.BackgroundColor3 = g.YN
		end
	end;
	local function n(a)
		k.Text = a and "MULTI: ON" or "MULTI: OFF";
		k.BackgroundColor3 = a and g.BB or g.BN
	end;
	f.MouseButton1Click:Connect(function()
		local a = a:TA()
		l(a)
	end)
	h.MouseButton1Click:Connect(function()
		local a = a:TMode()
		m(a)
		i(a)
	end)
	k.MouseButton1Click:Connect(function()
		local a = a:TM()
		n(a)
	end)
	d.MouseButton1Click:Connect(function()
		j = not j;
		d.Text = j and "UI: LOCKED" or "UI: UNLOCKED";
		d.BackgroundColor3 = j and g.GN or g.GB
	end)
	e.InputBegan:Connect(function(d, e)
		if e then
			return
		end;
		if d.KeyCode == b then
			local a = a:TA()
			l(a)
		elseif d.KeyCode == c then
			local a = a:TM()
			n(a)
		end
	end)
	local a = a:TMode()
	m(a)
	i(a)
end;
j()
